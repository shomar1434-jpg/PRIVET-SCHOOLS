# مرحلة سير الاعتمادات والطلبات وربطها بالحضور والانضباط

## الدورة المنفذة
1. المستخدم ينشئ طلبًا من: إجازة / استئذان / غياب.
2. الطلب ينتقل إلى الوكيل المسؤول فقط.
3. الوكيل يختار: اعتماد / إعادة للتعديل / رفض.
4. الإعادة للتعديل تتطلب سببًا مكتوبًا، وتعيد الطلب للموظف قابلًا للتعديل وإعادة الإرسال.
5. رفض الوكيل يغلق الطلب ولا يرسله للمدير.
6. اعتماد الوكيل ينقل الطلب إلى المدير.
7. المدير يختار: اعتماد نهائي / رفض.
8. الاعتماد النهائي فقط ينشئ حركة في `staff_discipline_movements`.
9. قيد UNIQUE على `request_id` يمنع إنشاء حركة انضباط مكررة لنفس الطلب.
10. صفحة الحضور والانضباط تزامن الحركات المعتمدة إلى السجل اليومي وتمنع تعديلها/حذفها يدويًا لأنها مرتبطة بقرار اعتماد.

## سجل التدقيق
كل انتقال يحفظ في `employee_request_events`: الإرسال، اعتماد الوكيل، الإعادة للتعديل وسببها، رفض الوكيل، إعادة الإرسال، اعتماد المدير النهائي، رفض المدير.

## العزل
كل استعلام وتسجيل مربوط بـ `school_id` وعضوية نشطة. الجداول الجديدة محمية بـ RLS، وتم سحب صلاحيات القراءة/الكتابة المباشرة من anon/authenticated؛ التشغيل عبر Edge Function موثقة فقط.

## Supabase المنشور
- migration: `private_school_employee_request_approval_discipline`
- Edge Function: `private-school-workflows` version 4

## الفحوص الداخلية
- فحص Syntax لملفات JavaScript: PASS
- فحص حالات الدورة وقواعد الرفض/الإعادة: PASS
- فحص قيد منع التكرار على request_id: PASS
- فحص عدم وجود بيانات اختبار متبقية: PASS
- فحص عدم السماح المباشر للجداول الجديدة من anon/authenticated: PASS
- فحص ربط manager/agent بصفحة الانضباط في وضع privateEdition: PASS
- فحص منع تعديل/حذف الحركة الناتجة من اعتماد: PASS

## بوابة الإغلاق النهائية
تحتاج تجربة مدرسة مستقلة تحتوي على مستخدم + وكيل + مدير؛ البيئة الحالية في قاعدة البيانات لا تحتوي مدرسة نشطة فيها الأدوار الثلاثة معًا، لذلك الاختبار الحقيقي End-to-End يتم بعد رفع هذه الحزمة إلى مدرسة الاختبار، ولا تنتقل المرحلة التالية قبل نجاحه.
