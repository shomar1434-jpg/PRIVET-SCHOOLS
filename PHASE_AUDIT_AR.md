# مراجعة المرحلة: البنية التشغيلية للمستخدمين في المدارس الخاصة

## ما تم إغلاقه في هذه المرحلة

### 1. العزل الصارم بين المدارس
- تم تطبيق مساحة عمل محلية منفصلة لكل `school_id`.
- عند الانتقال من مدرسة إلى أخرى تحفظ بيانات المدرسة المغادرة في خزنتها، تنظف المفاتيح التشغيلية المحلية، ثم تستعيد بيانات المدرسة الجديدة فقط.
- وضع مدير النظام مستثنى من سياق المدرسة ولا يُعامل كعضوية مدرسة.
- خدمات Supabase الحساسة التي تمت مراجعتها: المراسلات، الملفات، التكليفات، سير الطلبات، الأداء الوظيفي؛ وجميعها تتحقق من العضوية النشطة وتقيد العمليات بـ `school_id`.

### 2. المستخدم المشترك والمدير المشترك
- Auth identity واحدة يمكن أن تملك عضويات متعددة في مدارس مختلفة.
- التسجيل في مدرسة ثانية لا ينشئ هوية Auth جديدة إذا كان الحساب موجودًا؛ يتم التحقق من كلمة المرور الحالية ثم إضافة عضوية جديدة.
- دعوات المدارس يمكن أن تربط الهوية الموجودة بمدرسة أخرى.
- حذف عضوية من مدرسة لا يحذف Auth إذا بقيت عضويات أخرى.
- `private-school-session` يعيد جميع المدارس والأدوار النشطة للحساب ويختار السياق بواسطة `schoolId + role`.

### 3. الأدوار المزدوجة
- المدير يستطيع إضافة دور إضافي لنفس المستخدم دون إزالة دوره الأصلي.
- كل دور يمثل Membership مستقلة داخل نفس المدرسة.
- مبدل المدرسة/الدور لا يظهر إلا عند وجود أكثر من عضوية/دور.
- التبديل يثبت جلسة Supabase أولًا ثم يغير السياق المحلي؛ لا تبديل محلي قبل نجاح السياق السحابي.

### 4. منطق الوكيل
- التصنيفات المرجعية الموحدة:
  - `educational` = وكيل/وكيلة الشؤون التعليمية
  - `school_affairs` = وكيل/وكيلة الشؤون المدرسية
  - `student_affairs` = وكيل/وكيلة شؤون الطلاب
- التصنيف لا يختاره المستخدم في التسجيل؛ يحدده المدير.
- لا يمكن تفعيل وكيل بلا تصنيف.
- قسم الوكيل والسجلات يعرضان سجلات التصنيف المحدد + السجلات المشتركة فقط.
- محاولة فتح سجل خارج نطاق الوكالة تُرفض.
- القيمة القديمة `educational_affairs` تُحوّل تلقائيًا إلى `educational` للتوافق.

### 5. الرئيسية والخروج
- تم الاحتفاظ بـ `school_navigation_guard.js` من التصحيح المعتمد:
  - مدير النظام له مسار رئيسية/خروج مستقل.
  - المستخدم المدرسي يعود داخل نفس المدرسة عند الرئيسية، ويُنهي جلسته ويعود إلى بوابة نفس المدرسة عند الخروج.

## حالة Supabase المنشورة
- `private-school-registration` → **v3**
- `private-school-invite` → **v6**
- `private-school-manager` → **v5**
- `private-school-session` → **v4**

## الفحوص الداخلية
- فحص JavaScript للملفات المستقلة: {"private-school-bridge.js": "PASS", "private-school-runtime.js": "PASS", "private-school-isolation.js": "PASS", "private-multi-school-switcher.js": "PASS", "school_navigation_guard.js": "PASS"}
- تحقق ثابت من وجود دعم `add_member_role`: PASS
- تحقق من التصنيفات الثلاثة للوكيل: PASS
- تحقق من fail-closed عند غياب تصنيف الوكيل: PASS
- تحقق من تخزين جميع Memberships في الجلسة: PASS
- تحقق من تحميل طبقة العزل ومبدل المدرسة/الدور من runtime: PASS
- تحقق من إزالة تصنيف الوكيل من التسجيل: PASS
- تحقق من عدم وجود أي اعتماد على مسار ملفات نسخة المدارس العامة: PASS
- تمت محاكاة تبديل مساحة العمل بين مدرستين سابقًا ونجح حفظ/استرجاع بيانات كل مدرسة دون تسرب.

## الملفات التشغيلية في الحزمة
- `private-school-bridge.js`
- `private-school-runtime.js`
- `private-school-isolation.js`
- `private-multi-school-switcher.js`
- `manager.html`
- `agent.html`
- `wakil-records.html`
- `private-school-user-register.html`
- `school_navigation_guard.js`

> لا توجد داخل الحزمة ملفات من نسخة المدارس العامة ولا روابط تعتمد عليها.
