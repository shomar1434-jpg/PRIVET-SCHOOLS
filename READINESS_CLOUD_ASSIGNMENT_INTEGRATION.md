# تكامل مهام الجاهزية مع مركز التكليفات

تم في 2026-08-17 ربط محرك الجاهزية بمحرك التكليفات السحابي الموحد.

- مهام مدير المدرسة تبقى داخل محرك الجاهزية ولا تنشأ كتَكليف خارجي.
- أعضاء فريق الجاهزية الآخرون يرتبطون بحساب مستخدم فعلي داخل المدرسة (`user_id` + email).
- كل مسؤولية مسندة تنشئ تكليفاً سحابياً واحداً في `central_tasks` مع `module_key=school_readiness` وبيانات المصدر داخل metadata.
- تظهر المهمة للمكلف من شريط التكليفات الإضافية ومركز تكليفاتي.
- مساحة مهمة الجاهزية تطلب وصف التنفيذ وشاهداً إلزامياً.
- الملف يرفع عبر CloudFileEngine داخل bucket المدرسة ويرتبط بالمهمة عبر `central_task_evidence`.
- `platform-tasks` يعكس تحديث التنفيذ والشاهد والتعثر والاعتماد مباشرة إلى `school_readiness_plans.readiness_data`.
- إزالة الإسناد من فريق الجاهزية تنهي التكليف السحابي القديم.
- إعادة الإسناد تنقل نفس التكليف إلى المستخدم الجديد بدلاً من تكراره.

يتطلب النشر السحابي نشر النسخة الجديدة من Edge Function: `platform-tasks`.
