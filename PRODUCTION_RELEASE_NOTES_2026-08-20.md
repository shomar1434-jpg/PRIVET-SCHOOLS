# إصدار الإنتاج — المدارس الأهلية والخاصة

هذه الحزمة مخصصة للرفع إلى مستودع GitHub الخاص بنظام المدارس الأهلية والخاصة.

## حالة التشغيل
- لا يوجد محرك تجارب محلية أو Sandbox ضمن الحزمة.
- لا توجد بيانات مدارس أو مستخدمين تجريبية.
- لا توجد واجهات أو رسائل تشير إلى وضع محلي أو تجريبي.
- إعداد Supabase مستقل ويحتاج تعبئة Project Ref وPublishable Key في `standalone-private-config.js`.
- وظائف Supabase المطلوبة موجودة داخل `supabase/functions`.

## مراسلات مالك المدرسة
مراسلات مالك المدرسة مقيدة بمدير/مديرة المدرسة فقط:
- واجهة المستخدم تعرض المدير/المديرة فقط.
- Edge Function `private-school-messages` تتحقق من القاعدة على الخادم وتمنع إرسال المالك لأي دور آخر.
