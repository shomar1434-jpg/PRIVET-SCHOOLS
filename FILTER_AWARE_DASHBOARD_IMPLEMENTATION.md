# تنفيذ الفلاتر التراكمية للوحة القيادة

تم اعتماد النسخة الكاملة الأخيرة كمرجع، مع الحفاظ على استيراد Supabase بصيغة npm في جميع Edge Functions.

## ما تم تنفيذه
- إضافة خيار اليوم إلى فلتر الفترة مع إبقاء الأسبوع/الشهر/الفصل/العام.
- تمرير الفلاتر الحالية من الواجهة إلى Platform Core بدل قراءة نشاط 30 يومًا ثابتًا.
- تطبيق نطاق التحليل على `execution_role` للأحداث السحابية.
- تطبيق مصدر التركيز على أحداث التنفيذ: جميع المصادر، التكليفات، متابعة الوكيل، التوجيه الطلابي، التقويم/الأداء.
- تطبيق الفترة الزمنية على `occurred_at` لأحداث التنفيذ وعلى `measured_at` للمؤشرات.
- إبقاء طريقة العرض عاملاً في شكل وعمق القراءة، مع عدم تغيير مجموعة البيانات الأساسية خارج الفلاتر الأخرى.
- منع ظهور الإنجاز التشغيلي السحابي إذا كانت بياناته لا تطابق الفلاتر الحالية أثناء انتظار تحديث الشبكة.
- تحديث الجسر السحابي تلقائيًا عند تغيير أي فلتر.
- الحفاظ على `npm:@supabase/supabase-js@2` وعدم إعادة `esm.sh`.

## الملفات المعدلة
- school_command_center.html
- platform-core-engine.js
- platform-dashboard-bridge.js
- supabase/functions/platform-core/index.ts

لا يوجد SQL جديد لهذه المرحلة.
