# استكمال ربط نظام الاجتماعات بالسحابة

تم استكمال التطوير من النقطة السابقة على آخر نسخة مصححة، مع اعتماد جدول `public.meetings` في Supabase كمصدر أساسي للاجتماعات.

## ما تم تنفيذه

- جعل الحفظ في Supabase يسبق التخزين المحلي.
- الإبقاء على `localStorage` كذاكرة مؤقتة ونسخة احتياطية عند تعذر الاتصال فقط.
- إعادة محاولة مزامنة الاجتماعات المحلية غير المتزامنة تلقائيًا عند فتح قاعة الاجتماعات.
- جعل القراءة من Supabase هي المرجع عند تحميل الأرشيف، مع تحديث الذاكرة المؤقتة من البيانات السحابية.
- استكمال خريطة حقول جدول `meetings`، وتشمل المحضر والحضور والمرفقات والملاحظات والاعتماد.
- إضافة تحديث سحابي للاجتماع بدل إنشاء نسخ مكررة.
- ربط اعتماد المحضر فعليًا بالحقول:
  - `status = approved`
  - `approved_by`
  - `approved_at`
  - `updated_at`
- منع اعتبار المحضر معتمدًا أو إرساله إذا فشل تحديث الاعتماد في Supabase.
- حذف الاجتماع من Supabase أولًا ثم حذف النسخة المؤقتة، لمنع اختلاف الأرشيف المحلي عن السحابي.
- حفظ هوية المشاركين باستخدام:
  - `microsoft_user_id`
  - `microsoft_email`
  - `user_id`
  - الاسم والدور
- توحيد جسر قاعة الاجتماعات مع طبقة المراجعة والطباعة لمنع تكرار منطق الحفظ.

## دورة العمل الحالية

إنشاء الاجتماع → حفظ في Supabase → ذاكرة مؤقتة محلية → مراجعة واعتماد → تحديث السجل نفسه في Supabase → الطباعة أو الإرسال → الظهور في الأرشيف والمحفظة.

## قاعدة المطابقة

1. Microsoft Object ID.
2. Microsoft Teams Email داخل المدرسة نفسها.
3. لا تتم المطابقة الآلية بالاسم.

## ملاحظة النشر

النسخة جاهزة لرفع محتوياتها إلى مستودع GitHub المرتبط بالنسخة السحابية. لا يلزم إنشاء جدول اجتماعات جديد؛ تعتمد النسخة على جدول `meetings` الحالي والحقول التي تم تجهيزها سابقًا.
