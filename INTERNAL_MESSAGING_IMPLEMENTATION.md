# نظام المراسلات الداخلية – المرحلة الأولى

تمت إضافة:
- مركز مراسلات مستقل `internal_messages.html`.
- صندوق وارد ومرسل، عداد غير المقروء، البحث، الرد، والأولوية.
- اختيار المستلمين من مستخدمي المدرسة نفسها.
- زر مراسلات ثابت في واجهات المدير والوكيل والمعلم والموجه/الموجهة الطلابية ورائد النشاط والموظف الإداري.
- عزل كامل حسب `school_id` والتحقق من جلسة المنصة عبر Edge Function.
- Edge Function: `platform-messages`.
- مخطط قاعدة البيانات: `SUPABASE_INTERNAL_MESSAGING.sql`.
- GitHub Action ينشر `platform-messages`.

مهم قبل الاختبار السحابي:
1. نفّذ ملف `SUPABASE_INTERNAL_MESSAGING.sql` مرة واحدة في SQL Editor في Supabase.
2. ارفع النسخة إلى GitHub؛ الـ workflow سينشر `platform-messages`.
3. سجّل الدخول بحسابين من المدرسة نفسها لاختبار الإرسال والقراءة.

التطوير التالي:
- مرفقات من الجهاز ومكتبة القسم.
- تعميم رسمي وتأكيد الاطلاع.
- ربط الرسائل بالتكليفات والتقارير وبطاقات الأداء.
