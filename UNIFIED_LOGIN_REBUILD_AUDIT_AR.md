# إعادة بناء الدخول الموحد للمدارس الخاصة

- تم العمل على النسخة الكاملة المرفوعة نفسها، وليس على Patch سابق.
- رابط المدرسة الموحد: `school-login.html?edition=private&schoolId=<school_id>`.
- نفس الرابط يستخدمه المدير والوكيل والمعلم والموجهون والإداري بعد التفعيل.
- رابط التسجيل مستقل ولا يستخدم للدخول.
- ألغيت طبقة `private-storage-namespace.js` من صفحات HTML لأن النسخة الخاصة في حاوية مستقلة؛ العزل يعتمد على `school_id` والعضويات والخدمات الخلفية.
- `private-manager-login.html` أصبح تحويل توافق فقط إلى رابط المدرسة الموحد.
- حارس الصفحة لا يعيد التوجيه تلقائيًا عند خطأ الجلسة؛ يعيد المحاولة ثم يغلق الصفحة بلوحة آمنة، لمنع حلقة الدخول.
- `supabase-bridge.js` القديم لا ينشئ جلسة Auth ثانية في الإصدار الخاص؛ يعيد استخدام `PrivateSchoolBridge`.
- تم الحفاظ على فحص الالتزام، سير الاعتمادات، الانضباط، المراسلات، التكليفات، منطق الوكيل، والأدوار/المدارس المتعددة الموجودة في النسخة المرجعية.

## Supabase المنشور
- `private-school-owner` → v7
- `private-school-provisioning` → v6
