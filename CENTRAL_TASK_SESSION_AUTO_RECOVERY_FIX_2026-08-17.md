# إصلاح التجديد التلقائي لجلسة مركز التكليفات

- السبب: `PlatformCloudSession.ensure()` كان يرفض الجلسة المنتهية مباشرة ويطلب تسجيل الخروج بدل محاولة تجديدها.
- أضيف `action=renew` إلى `platform-session` مع تدوير آمن للرمز وفترة سماح للتجديد، والتحقق من استمرار عضوية المستخدم في المدرسة.
- أضيف `PlatformCloudSession.recover()` مع مسارين: تجديد الرمز السابق، ثم إعادة إنشاء الجلسة من سياق حساب المدرسة عند الحاجة.
- `cloud-file-engine.js` و`cloud-task-engine.js` يعيدان محاولة الطلب مرة واحدة بعد 401 عقب تجديد الجلسة.
- تم اختبار حالات: جلسة صالحة، جلسة منتهية قابلة للتجديد، وفشل التجديد ثم الاستعادة من بيانات الحساب.
- لا توجد Migration مطلوبة؛ مخطط `platform_sessions` الحالي يحتوي الحقول اللازمة.
