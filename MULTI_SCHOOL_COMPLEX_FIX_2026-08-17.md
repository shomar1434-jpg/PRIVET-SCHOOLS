# إصلاح دعم المجمعات التعليمية — 2026-08-17

تمت إعادة دعم الحساب الواحد كمدير لأكثر من مدرسة مستقلة مع الفصل التام بين سياقات المدارس.

## التعديلات
- السماح لمدير النظام بإنشاء أكثر من مدرسة بنفس بريد المدير، مع منع تكرار اسم المدرسة فقط.
- إعادة استخدام هوية المدير نفسها بدل إنشاء حساب Auth جديد لكل مدرسة.
- إنشاء عضوية `school_members` مستقلة لكل مدرسة ودور.
- توسيع محرك `platform-session` لاكتشاف مدارس المدير من `school_members` و`users` و`schools.manager_email` للتوافق مع المدارس القديمة والجديدة.
- دمج مصادر العضويات في `multi_school_switcher.js` بدل إيقاف البحث عند أول مصدر.
- إعادة إظهار حالة «المدرسة النشطة / غير نشطة» في واجهة التبديل.
- الحفاظ على عزل مساحة التخزين المحلية والسحابية حسب `school_id` أثناء التبديل.
- حماية الحساب المشترك عند حذف مدرسة من مجمع: لا يُحذف حساب Auth إذا كانت له عضوية فعالة في مدرسة أخرى، ويتم نقل المدرسة الافتراضية إلى عضوية أخرى عند الحاجة.

## الملفات المعدلة
- `multi_school_switcher.js`
- `supabase/functions/platform-session/index.ts`
- `supabase/functions/system-admin/index.ts`

## التحقق
- اجتاز `multi_school_switcher.js` فحص بناء JavaScript عبر `node --check`.
- بقي نشر Edge Functions المعدلة مطلوبًا عند رفع هذه النسخة إلى البيئة السحابية حتى تعمل تغييرات الإنشاء والجلسة من الخادم.
