# تقرير المراجعة الداخلية — إصدار المدارس الأهلية والخاصة

## الأساس
- مرجع العمل: `platform_v39_reference_github_pages_final_fixed.zip`.
- تم إبقاء مسار التعليم العام ومشروعه البرمجي منفصلين.
- مشروع المدارس الخاصة: `PRIVET-SCHOOLS`، المرجع `okjwdzvnqsdetxdsvdgr`.

## التصحيحات المنفذة
- استبدال اعتماد نسخة المدارس الخاصة على الجداول القديمة `users` بجسر منفصل يعتمد Supabase Auth وعضويات `school_members` و`school_owners` والدعوات.
- دورة إنشاء المدرسة والمالك من مدير النظام.
- دخول المالك وتفعيل لوحة المالك.
- إنشاء دعوة مدير، تسجيل المدير Pending، تفعيل المدير من المالك، ودخول المدير بعد التفعيل.
- إدارة المدير لدعوات المستخدمين وتفعيلهم، مع أنواع الأدوار المعتمدة.
- صلاحية الوكيل لإنشاء/تفعيل الموظف الإداري التابع له فقط، مع بقاء الحماية الخلفية في Edge Function.
- تفعيل أقسام فحص الالتزام، هوية وقالب المدرسة، والمخرجات المؤسسية للمالك والمدير.
- فحص الالتزام يستخدم 19 متطلباً، ويدعم السنوات 1448هـ–1548هـ، ويمنع المدير من تعديل متطلبات المالك الحصرية.
- مزامنة معرف المدرسة إلى مفاتيح النسخة القديمة داخل الجلسة الحالية فقط لتشغيل الأقسام المرجعية من دون خلط المدارس.
- خروج آمن يستدعي Supabase signOut ويزيل مفاتيح جلسة المدرسة فقط، من دون `localStorage.clear()`.
- اعتراض مسارات الخروج القديمة في manager/agent/student_advisor/activity_leader عند تشغيل النسخة الخاصة.
- إضافة مدخل إصدار المدارس الخاصة إلى `index.html` من دون إزالة مدخلات التعليم العام.

## التحقق الخلفي
- `schools`: 0 سجلات قبل التسليم.
- `school_owners`: 0 سجلات قبل التسليم.
- `school_members`: 0 سجلات قبل التسليم.
- `school_invites`: 0 سجلات قبل التسليم.
- يوجد System Admin نشط واحد.
- RLS مفعّل على: schools, school_owners, school_members, school_invites, compliance_inspections, school_template_profiles, private_school_outputs.
- لم تُجر أي Migration أو DDL في هذه الجولة.

## التحقق البرمجي
- `private-supabase-bridge.js`: اجتاز `node --check`.
- جميع JavaScript الداخلي في صفحات `private-*.html`: اجتاز `node --check`.
- المحولات المضافة إلى manager/agent/teacher/student_advisor/activity_leader: اجتازت فحص JavaScript المستقل.
- لا يوجد `service_role` أو مفتاح سري في ملفات `private-*`.
- لا يوجد مرجع إلى مشروع Supabase العام/القديم داخل ملفات `private-*`.
- لا يوجد `localStorage.clear()` داخل ملفات `private-*`.
- تمت إضافة تبعيات بوابتي الموجه الطلابي ورائد النشاط المفقودة من v39.

## حدود الاختبار
- لم يتم إنشاء حسابات/مدارس اختبار في قاعدة البيانات حتى لا نضيف بيانات تشغيلية دون اعتماد المستخدم.
- الاختبار HTTP من بيئة الملفات المحلية لم يكتمل بسبب عدم توفر DNS الخارجي داخل الحاوية؛ لذلك لم يسجل كاختبار ناجح.
- تم التحقق من المشروع والجداول وEdge Functions مباشرة عبر موصل Supabase.
