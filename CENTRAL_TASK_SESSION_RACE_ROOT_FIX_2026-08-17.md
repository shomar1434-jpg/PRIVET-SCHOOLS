# إصلاح سباق الجلسات السحابية – 2026-08-17

تم تحديد السبب الجذري من سجلات Supabase الحية: طلبات متزامنة إلى platform-session كانت تنشئ أكثر من جلسة، بينما كل تسجيل دخول كان يلغي جميع الجلسات النشطة لنفس المستخدم/المدرسة. وصول الردود بترتيب مختلف كان يجعل المتصفح يحتفظ أحيانًا برمز ملغى فورًا، ثم تظهر 401 في platform-session/platform-state/platform-messages ومركز التكليفات.

الإصلاحات:
- منع الإلغاء الشامل للجلسات عند تسجيل الدخول.
- عدم إلغاء الرمز السابق أثناء renew؛ يترك لينتهي طبيعيًا عبر expires_at.
- قفل open/recover في المتصفح لمنع الطلبات المتزامنة المكررة.
- عدم حذف الرمز السليم قبل نجاح تسجيل جلسة بديلة.
- إعادة المحاولة مرة واحدة بعد 401 في task/file/state/core/messages.
- إزالة استعلامات توافقية على أعمدة مدارس غير موجودة، والاعتماد على manager_email الفعلي.

الاختبارات:
- اختبار تزامن open: طلب واحد فقط عند استدعاءين متزامنين – PASS.
- اختبار تزامن recover/ensure: طلب renew واحد فقط – PASS.
- JavaScript syntax لجميع ملفات JS – PASS.
