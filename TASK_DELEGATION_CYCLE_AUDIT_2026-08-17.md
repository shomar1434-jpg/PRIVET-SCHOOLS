# مراجعة دورة تفويض التكليفات — 2026-08-17

تمت مراجعة المسار: إنشاء → ظهور للمكلف → تنفيذ/تحديث → رفع شاهد → إرسال للاعتماد → اعتماد/إعادة/رفض → سحب الصلاحيات عند الإنهاء.

## عائق تم اكتشافه وتصحيحه
كان إنشاء التكليف يتحقق من `users.school_id` فقط. هذا يمنع إسناد التكليف إلى مستخدم يعمل في المدرسة الحالية عبر `school_members` إذا كانت مدرسته الأساسية مختلفة. تم تعديل `platform-tasks` ليقبل المستخدم إذا كان مرتبطًا مباشرة بالمدرسة أو لديه عضوية فعالة في `school_members` للمدرسة الحالية.

## تحقق الكود
- create: موجود وسحابي.
- list/get: يقيّد المكلف بحسابه والمدرسة.
- add-update: يحفظ التنفيذ ويحوّل المهمة إلى in_progress.
- attach-evidence: يربط شاهد التنفيذ بملفات المدرسة.
- pending_approval: انتقال المكلف للاعتماد.
- approved / returned / rejected: محكومة بصاحب الصلاحية.
- central_task_reviews / central_task_events: توثيق المراجعة والأحداث.
- task_access_grants: تمنح عند الإسناد وتسحب عند الحالات النهائية.
- فحص JavaScript للواجهة والمحول السحابي: ناجح.

## حدود الاختبار
لم يتم تنفيذ اختبار حي بحسابين فعليين مختلفين في هذه الجلسة لأن أداة قاعدة البيانات المتاحة رفضت قراءة بيانات `central_tasks` بصلاحية MCP. لذلك لا يُوصف هذا التقرير بأنه إثبات E2E حي عبر حساب مدير وحساب مكلف؛ بل مراجعة تكاملية للكود مع إزالة عائق فعلي مكتشف.
