# تقرير استقرار التخزين السحابي وربط شواهد الجاهزية

تم تنفيذ المراجعة النهائية على نسخة المنصة المرجعية، وشملت:

- إعادة بناء `platform-session` دون الاعتماد على عمود غير موجود مثل `users.name`.
- مطابقة المستخدم داخل المدرسة بواسطة `school_id` وحقول الهوية الموجودة فعليًا في سجل المستخدم.
- دعم الحسابات المرتبطة بـ Supabase Auth والحسابات القديمة المخزنة في `public.users`.
- التحقق من صلاحية المدرسة والمستخدم والعضوية قبل إنشاء جلسة الملفات.
- منع تراكم الجلسات النشطة للمستخدم نفسه.
- إضافة رموز أخطاء و`requestId` لتسهيل التشخيص في سجلات Edge Functions.
- حماية استعلامات عرض الملفات والمجلدات من كشف ملفات مستخدم آخر داخل المدرسة.
- السماح للمستخدم العادي برؤية ملفاته والملفات المدرسية المشتركة فقط، مع صلاحية المدير للإدارة الموسعة.
- التحقق من حجم الملف ونوعه في Edge Function قبل الرفع.
- تنظيف الملف وسجل البيانات عند فشل إنشاء الرابط أو سجل شاهد الجاهزية.
- ربط شاهد الجاهزية بـ `platform_files` و`school_readiness_evidence` و`platform_file_links`.
- منع رفع شاهد سحابي قبل وجود `plan_id` سحابي صالح لخطة الجاهزية.
- مزامنة الحذف والاستعادة والحذف النهائي لشاهد الجاهزية.
- دمج ترحيل شواهد الجاهزية داخل ملف الإعداد النهائي الموحد.

## الاختبارات المنفذة

- فحص JavaScript الساكن للملفات العامة.
- فحص تكامل صفحات محرك الملفات.
- التحقق من عدم تكرار تحميل ملفات المحرك داخل الصفحات المربوطة.
- التحقق من وجود وظائف Supabase والملفات المطلوبة للنشر.

## اختبار القبول المطلوب بعد الرفع

1. تسجيل الدخول بحساب مدير مدرسة مستقلة وظهور سجل في `platform_sessions`.
2. إنشاء مجلد ورفع ملف من مكتبة المدير.
3. رفع شاهد من مركز الجاهزية وظهوره في:
   - `platform_files`
   - `platform_file_links`
   - `school_readiness_evidence`
   - Storage bucket `school-platform-files`
4. إعادة فتح الصفحة ومعاينة الشاهد.
5. اختبار مدرسة ثانية والتأكد من عدم ظهور ملفات المدرسة الأولى.
6. اختبار معلمين داخل المدرسة نفسها والتأكد من عدم تداخل ملفاتهما الشخصية.
