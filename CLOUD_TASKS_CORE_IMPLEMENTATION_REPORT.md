# تقرير تنفيذ نواة التكليفات السحابية

تم تنفيذ الحزمة الأولى على آخر نسخة مصححة، وتشمل:

- مخطط SQL موحد للتكليفات والإسناد والتحديثات والمراجعات والشواهد والإشعارات والتفويضات المؤقتة.
- Edge Function باسم `platform-tasks` تعتمد على جلسة المنصة نفسها وتفرض عزل `school_id`.
- محرك واجهة موحد `cloud-task-engine.js`.
- محول مرحلي لمركز التكليفات `central-task-cloud-adapter.js` يحول الإنشاء والتحميل والنقل وتغيير الحالة ورفع التنفيذ والشاهد إلى Supabase.
- ربط شواهد التكليف بمحرك الملفات الموحد دون نسخ الملفات.
- إعداد أولي لمنح صلاحيات مؤقتة للسجلات/الوحدات المرتبطة بالتكليف.
- Views أولية لمؤشرات المدرسة وحمل العمل.
- تحديث GitHub Actions لنشر `platform-tasks`.

## خطوات النشر
1. تنفيذ `SUPABASE_CLOUD_TASKS_CORE.sql` مرة واحدة في SQL Editor.
2. رفع النسخة إلى GitHub وانتظار نجاح Workflow.
3. التأكد من ظهور `platform-tasks` في Supabase Edge Functions.
4. تسجيل الخروج والدخول لإنشاء جلسة حديثة.
5. اختبار إنشاء تكليف من المدير وفتحه من حساب المكلف.

## نطاق هذه الحزمة
هذه هي نواة العلاقات السحابية. الربط الكامل لبطاقة «تكليفاتي الإضافية» ومساحة التكليف واللوحات التنفيذية سيُبنى فوق هذه النواة في الحزم التالية.
