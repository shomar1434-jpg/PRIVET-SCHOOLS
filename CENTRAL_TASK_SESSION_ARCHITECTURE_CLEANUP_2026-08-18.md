# تنظيف معماري نهائي لجلسة مركز التكليفات

## السبب الجذري المثبت في النسخة المرفوعة
1. school-login كان يسمح بالدخول حتى عند فشل PlatformCloudSession.open.
2. platform-session كان يلغي كل الجلسات النشطة لنفس المستخدم والمدرسة عند كل تسجيل دخول.
3. agent-core-v2 كان يدير نفس رمز الجلسة بصورة مستقلة: يكتب ويحذف الرمز ويستخدم bootstrap_auth ومسار كلمة مرور قديم.
4. PlatformCloudSession كان يحتوي fallback لكلمة مرور من currentUser، ما سمح بتعدد مسارات الاستعادة.

## المعالجة
- PlatformCloudSession هو المالك الوحيد لدورة حياة رمز الجلسة.
- تسجيل الدخول لا يكتمل إلا بعد إنشاء جلسة سحابية ناجحة.
- تسجيل دخول جديد لا يلغي جلسات التبويبات/الأجهزة الأخرى.
- renew/switch فقط يدوران الجلسة المحددة صراحة.
- Agent Core يستهلك جلسة PlatformCloudSession ولا يعدل الرمز بنفسه.
- لا يوجد fallback لكلمة مرور مخزنة محليًا.
- الاستعادة المتزامنة deduplicated عبر recoveryPromise.

## اختبارات
- فتح جلسة وحفظ الرمز: PASS
- ensure على جلسة صالحة بدون طلب شبكة زائد: PASS
- renew: PASS
- 3 renew متزامنة -> طلب واحد: PASS
- غياب الرمز -> فشل صريح بلا password fallback: PASS
- 54 JS خارجي + 316 inline JS: PASS
- عدد الملفات المعدلة من النسخة المرفوعة: 5 فقط.
