# اعتماد مركز المعلومات المدرسية كمزود بيانات مركزي

## النتيجة
- إجمالي صفحات HTML: 67
- الصفحات الداخلية المرتبطة بمحرك مركز المعلومات: 63
- الصفحات العامة/الإعداد المستثناة: 6
- الصفحات التي تحتوي إشارات دلالية إلى طلاب/معلمين/موظفين/أعضاء/مكلفين: 58
- الصفحات المستهلكة غير المرتبطة: 0
- تضمينات المحرك المكررة: 0

## العقد المركزي
`SchoolInformationSource` يوفر:
- `getStudents()`
- `getStaff()`
- `getTeachers()`
- `getSnapshot()`
- `findStudent(query)`
- `findStaff(query)`
- `refresh()`
- `notifyUpdated()`
- `getSchoolId()`
- `getAcademicYear()`

## التزام المصدر
- الطلاب: جدول `students` + مرآة مركز المعلومات المحلية للمدرسة/العام عند العمل المحلي.
- المنسوبون: جدول/دليل `users` الخاص بالمدرسة.
- المعلمون: مشتقون من المنسوبين حسب الدور الوظيفي.
- كل القراءة معزولة حسب `school_id`.
- تحويل فصل نور الرقمي إلى الشعبة العربية يتم مركزيًا.

## التوزيع
لا يوجد «تصدير يدوي». الصفحات تقرأ المصدر مباشرة، ويقوم المحرك بالمسح الدلالي لحقول الطالب/المعلم/الموظف/عضو اللجنة/المجلس/المكلف/المسؤول وربطها بالمصدر.
عند تحديث مركز المعلومات يُطلق `school-information-updated` ويتم التحديث في الصفحة الحالية، كما تستخدم `BroadcastChannel` لتحديث الصفحات المفتوحة في تبويبات أخرى.

## مركز المعلومات
أضيفت لوحة «حالة التكامل مع المنصة» وتعرض أعداد الطلاب والمنسوبين والمعلمين والصفحات المرتبطة وحالة المصدر وآخر تحديث.
زر «تحديث البيانات في المنصة» يعيد قراءة المصدر المركزي ولا يصدر ملفًا.
