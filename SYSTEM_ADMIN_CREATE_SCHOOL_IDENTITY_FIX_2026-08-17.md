# إصلاح إنشاء المدارس والهوية الموحدة — 2026-08-17

## السبب المؤكد من سجلات Supabase
- Edge Function `system-admin` أعادت HTTP 500 عند إنشاء المدرسة.
- PostgreSQL سجّل: `duplicate key value violates unique constraint "users_email_key"`.
- Auth أنشأ المستخدم ثم حذفه مسار rollback بعد فشل العملية.
- السبب: النظام كان يبحث في `public.users` بالبريد **ودور manager معًا**، ثم ينشئ Auth. Trigger في المشروع قد ينشئ `public.users` تلقائيًا عند إنشاء Auth، وبعد ذلك كان الكود يحاول INSERT لنفس البريد مرة ثانية.

## التصحيح
1. البحث عن هوية المستخدم بالبريد فقط، بغض النظر عن دوره الحالي.
2. الدور داخل المدرسة الجديدة يُحفظ في `school_members` كـ `manager`، دون تغيير دور المستخدم في مدرسته الأصلية.
3. بعد إنشاء Auth لحساب جديد، يعاد فحص `public.users` لاعتماد الصف الذي أنشأه Trigger بدل إنشاء صف مكرر.
4. إكمال بيانات الصف الذي أنشأه Trigger (الاسم، كلمة المرور العامة، الحالة، ودور manager للهوية الجديدة فقط).
5. تنظيف آمن للهوية التي أُنشئت أثناء العملية إذا فشلت العملية لاحقًا، لمنع بقاء مستخدم يتيم.
6. إذا كان البريد موجودًا مسبقًا، يجب استخدام كلمة مرور الهوية الحالية نفسها.
7. تحسين عرض رسالة الخطأ في `index-app.js` لقراءة body الخاص بـ FunctionsHttpError بدل الاكتفاء برسالة non-2xx العامة.

## لا يوجد تغيير مطلوب في قاعدة البيانات
تم التحقق من وجود أعمدة `school_members` اللازمة لدعم تعدد المدارس والأدوار.

## النشر المطلوب
يجب نشر Edge Function `system-admin` بعد رفع الملفات إلى GitHub/Supabase.
