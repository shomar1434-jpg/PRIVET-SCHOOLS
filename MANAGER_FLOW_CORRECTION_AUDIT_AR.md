# تصحيح واجهة المدير — المدارس الخاصة

- تم إلغاء الاعتماد على `private-messages.html` كنظام مراسلات موازٍ في المسارات النشطة.
- تم توجيه المالك وبقية الأدوار إلى نظام المراسلات الأصلي `internal_messages.html`.
- تم تعديل `internal-messaging.js` ليستخدم `PrivateSchoolBridge.messages()` و`PrivateSchoolBridge.tasks()` في إصدار المدارس الخاصة مع إبقاء الواجهة الأصلية.
- تم نشر `private-school-messages v4` بقواعد الصلاحيات المعتمدة:
  - المالك → المدير فقط.
  - المدير → جميع المستخدمين المسموحين فرديًا أو جماعيًا، دون المالك.
  - الوكيل → المدير وجميع المستخدمين فرديًا أو جماعيًا، دون المالك.
  - بقية المستخدمين → الوكلاء فقط.
- زر الحوكمة `إدارة المستخدمين` تم تحويله إلى `دخول المستخدمين` ويرتبط بـ `school-login.html?edition=private&schoolId=<school_id>`.
- التسجيل والتفعيل يبقيان داخل إدارة المستخدمين ولا توجد دعوة ثانية من زر الدخول.
- زر `سير الاعتمادات` يحمل عدادًا حقيقيًا = الطلبات المعلقة لدى المدير + تقارير الأداء المعلقة لديه.
- تم توسيع جداول المراسلات الحالية دون حذف الرسائل الموجودة.
- اختبار SQL التراجعي للمراسلات انتهى بصفر بيانات اختبار متبقية.
- فحوص JavaScript للملفات المعدلة نجحت.
