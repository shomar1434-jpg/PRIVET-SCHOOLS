# تقرير تأمين مدير النظام — تنفيذ فعلي

## الثغرة التي تم العثور عليها
- كان `index-app.js` يعتبر المستخدم مدير نظام إذا كان الاسم `admin` أو `مدير` أو البريد يحتوي قيمة ثابتة، أو إذا تم وضع `is_admin_session=true` في localStorage.
- لم تكن بوابة مدير النظام تطلب كلمة مرور فعلية.
- إنشاء المدارس كان متاحًا من الواجهة العامة ويكتب مباشرة إلى `schools` و`users` من المتصفح.
- توجد مسارات قديمة أخرى للكتابة المباشرة؛ لذلك الاعتماد على إخفاء الزر وحده غير كافٍ.

## التصحيح المنفذ
1. إزالة منطق التعرف على مدير النظام من الاسم/البريد/localStorage.
2. إضافة كلمة مرور فعلية لبوابة مدير النظام واستخدام Supabase Auth.
3. إضافة Edge Function جديدة: `system-admin`.
4. الوظيفة تتحقق من JWT ثم من جدول `system_admins` قبل أي عملية إدارية.
5. نقل إنشاء المدرسة وقراءة قائمة المدارس في لوحة مدير النظام إلى الوظيفة المحمية.
6. تعطيل إنشاء مدرسة من البوابة العامة.
7. إضافة `system_admin_audit_log` لتسجيل العمليات الإدارية.
8. إضافة SQL طوارئ يمنع anon/authenticated من INSERT/UPDATE/DELETE على `schools`.
9. تحديث GitHub Actions لنشر `system-admin`.
10. تعطيل الحذف النهائي داخل API الجديدة مؤقتًا إلى أن تتم مراجعة المدرسة غير المصرح بها قبل حذفها.

## مهم قبل الرفع
نفذ `SUPABASE_SYSTEM_ADMIN_SECURITY_LOCKDOWN.sql`، ثم أنشئ/حدد حساب مدير النظام في Supabase Authentication وأضف UUID الخاص به إلى `system_admins` باستخدام السطر المعلق أسفل SQL.

## ترتيب النشر
1. SQL الأمني.
2. إنشاء/تأكيد حساب Auth لمدير النظام وإضافته إلى `system_admins`.
3. رفع الحزمة إلى GitHub.
4. التأكد من نجاح `Deploy system-admin` في Actions.
5. اختبار: رابط الصفحة وحده لا يسمح بالدخول؛ مستخدم مدرسة لا يسمح له؛ مدير النظام المصرح له فقط يدخل.
6. بعد نجاح ذلك نراجع المدرسة المضافة ونحذفها وحساباتها بشكل منظم.
