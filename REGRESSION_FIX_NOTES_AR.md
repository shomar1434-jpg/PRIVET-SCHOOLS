# تصحيح تراكمي لمنع عودة مشكلة فتح السجلات المفوضة

المرجع: نسخة المنصة بعد تصحيح الأرشفة والحذف.

## السبب الجذري
كان Platform Core يثق في task_record_links الموجودة متى كانت غير فارغة، حتى لو كانت روابط قديمة عامة مثل shared/record. عند وجود رابط قديم واحد لم يكن ينتقل إلى metadata.delegatedRecords الدقيقة، ولذلك ظهرت بطاقة باسم record ولم يحصل حارس التفويض على سجل/Grant مطابقين.

## التصحيح
- metadata.delegatedRecords المولدة عند إنشاء التكليف أصبحت المصدر الأعلى أولوية للسجلات المفوضة.
- كل سجل يُتحقق منه مقابل platform_record_types النشط قبل عرضه.
- عند فتح مساحة التكليف، يعيد Platform Core تكوين أي task_record_link دقيق مفقود تلقائيًا.
- يعيد كذلك إنشاء task_access_grant دقيق للمكلف الحالي عند فقدانه.
- الروابط العامة أو القديمة لا تُعرض للمكلف ولا تستطيع توجيهه إلى سجل مدير/وكيل خاطئ.
- بقيت إصلاحات الأرشفة وحذف التكليف كما هي دون رجوع.
- بعد فتح السجل، delegated-access-guard يجد رابطًا وGrant متطابقين، فيتم إرسال record_opened ثم record_updated/record_completed، وبالتالي يتحرك progress_percent وتنعكس البيانات على اللوحات.

## لا يوجد SQL جديد
الإصلاح ذاتي في Platform Core ويقوم بالـ self-heal للتكليف النشط عند فتح مساحة التنفيذ.
