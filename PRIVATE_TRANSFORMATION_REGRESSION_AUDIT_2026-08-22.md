# المراجعة التراجعية الشاملة — إصدار المدارس الأهلية والخاصة

## النتيجة
- الفحوص الناجحة: **71 / 71**
- الفحوص الفاشلة: **0**
- تم الحفاظ على إصلاح الدخول الحالي وعدم إعادة بناء بوابة المصادقة.

## التصحيحات التي أُعيد تثبيتها
1. إزالة الشريط العلوي المكرر من واجهة مدير المدرسة فقط، مع بقاء الواجهة الأصلية وأيقوناتها.
2. زر «رابط دخول المستخدمين» في أعلى واجهة المدير يستخدم رابط المدرسة الموحد، وينسخه ثم يفتح بوابة المدرسة في تبويب مستقل.
3. إلغاء صفحة «دعوة مستخدم» القديمة؛ `private-manager-users.html` أصبح مسار توافق يعيد إلى إدارة المستخدمين الداخلية في `manager.html`.
4. إدارة الحسابات داخل المدير أصبحت للحسابات المسجلة فعليًا فقط؛ لا يوجد إنشاء مستخدم يدوي من المدير.
5. رابط تسجيل المستخدمين مستقل ويستخدم `PrivateSchoolBridge.registrationLink()`، وبعد التسجيل يبقى الحساب بانتظار التفعيل.
6. الوكيل يسجل بصفته «وكيل/وكيلة» فقط، والمدير يحدد تصنيف الوكالة قبل التفعيل.
7. رابط دخول المدير الذي يخرجه المالك أصبح هو نفسه رابط دخول المدرسة لجميع المستخدمين، دون `role=manager` في الرابط.
8. المراسلات عادت إلى مركز `internal_messages.html` الأصلي، مع سياسة المدارس الخاصة الخلفية.
9. أضيفت التسميات الجماعية العربية للمراسلات: الوكلاء/الوكيلات، المعلمون/المعلمات، الموجهون/الموجهات…
10. تحميل `private-report-identity-source.js` أعيد على صفحات الأدوار والمراسلات لضمان انعكاس القالب الموحد واسم/توقيع المستخدم.

## ما تم التحقق من بقائه سليمًا
- دورة الاعتمادات: مستخدم → وكيل (اعتماد/إعادة/رفض) → مدير (اعتماد نهائي/رفض).
- سبب الإعادة إلزامي، ويمكن للمستخدم تعديل الطلب وإعادة إرساله.
- رفض الوكيل لا يصل إلى المدير.
- الاعتماد النهائي فقط ينشئ حركة الانضباط، مع منع التكرار بواسطة `request_id`.
- مزامنة الحركات المعتمدة مع سجل الانضباط للمدير والوكيل.
- المراسلات: المالك → المدير فقط؛ المدير → جميع المستخدمين عدا المالك؛ الوكيل → المدير والمستخدمين؛ بقية المستخدمين → الوكيل فقط.
- الإرسال الجماعي متاح للمدير والوكيل.
- القالب الموحد: بيانات المدرسة والمالك والمدير والتواصل، توقيعا المالك والمدير، الختم الرقمي.
- الإعدادات المخصصة للمستخدم: الاسم والتوقيع الرقمي فقط.
- فحص الالتزام: لا يوجد «لا ينطبق»، ويدعم صورة/PDF، والاستبدال والإزالة، والطباعة الصارمة A4 مع توسيط الشاهد ومنع قص الصفحات.
- منطق الوكيل والتصنيفات الثلاثة والأدوار الإضافية محفوظ.
- منطق الرئيسية والخروج لمدير النظام ومستخدمي المدارس محفوظ.
- طبقة العزل الصارم حسب `school_id` محفوظة.
- طباعة وأرشيف التقويم الذاتي ما زالا موجودين ولم يتم استبدالهما.

## ملاحظة Supabase
تمت مقارنة المصدر المحلي مع الوظائف المنشورة:
- `private-school-owner` المنشورة v7 تعيد رابط المدرسة الموحد.
- `private-school-provisioning` المنشورة v6 تنشئ `schoolLoginPath` موحدًا.
لذلك لم تكن هناك حاجة لنشر نسخة خلفية جديدة في هذه المراجعة.

## بوابة الاختبار التالية
لا ننتقل إلى مرحلة أخرى حتى ينجح الاختبار الفعلي في مدرسة مستقلة:
1. المالك ينسخ رابط دخول المدرسة.
2. المدير يدخل ويبقى في قسمه.
3. زر رابط دخول المستخدمين ينسخ ويفتح نفس الرابط.
4. المستخدم يسجل من رابط التسجيل، يظهر Pending، يفعله المدير، ثم يدخل من رابط المدرسة نفسه.
5. تجربة طلب إجازة/استئذان/غياب عبر دورة الوكيل والمدير ثم فحص ظهوره في الانضباط.
