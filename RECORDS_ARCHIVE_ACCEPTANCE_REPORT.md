# تقرير التحقق الصارم — حفظ سجلات المدير والوكيل حسب مجلد السجل

## سبب الخلل الذي تمت معالجته
- كان نموذج سجل المدير يكتب مباشرة في localStorage ثم يعرض رسالة نجاح، دون عقد تحقق يثبت أن السجل ظهر فعلًا داخل مجلد `folderId` المرتبط بالسجل.
- نموذج سجلات الوكيل داخل iframe كان يرسل `postMessage` ثم يعرض رسالة نجاح فورًا، قبل أن تؤكد صفحة الوكيل أن السجل وصل للأرشيف أو دخل المجلد الصحيح.
- لم تكن هناك مرحلة موحدة للتحقق من المزامنة السحابية بعد الحفظ.

## التصحيح
- إضافة `records-archive-save-engine.js`.
- فرض مطابقة `folderId` مع كتالوج مجلدات الدور قبل قبول الحفظ.
- إعادة قراءة الأرشيف بعد الكتابة والتأكد أن السجل يظهر تحت فلتر المجلد نفسه.
- التحقق من المزامنة عبر PlatformPersistenceGuard / PlatformStateEngine عند توفرهما.
- المدير لا يعرض رسالة نجاح إلا بعد تحقق الحفظ.
- iframe سجلات الوكيل ينتظر ACK من صفحة الوكيل، ولا يعرض النجاح قبل اكتمال الحفظ والتحقق.
- لم يتم حذف أو ترحيل أي سجلات قديمة.

## نتيجة الاختبار التنفيذي الداخلي
`{"manager":{"folderId":"meetings","folderName":"سجل الاجتماعات","cloud":true,"count":2},"agent":{"folderId":"daily-duty-report","folderName":"تقرير المناوبة اليومي","cloud":true,"count":1},"invalidFolderRejected":true}`

## فحوص JavaScript
نجح Node syntax check للمحرك، ونموذج سجلات المدير، والقالب الداخلي لسجلات الوكيل، ومحرك أرشيف الوكيل.


## اختبار Supabase الفعلي
تم تنفيذ اختبار داخل معاملة `BEGIN ... ROLLBACK` على جدول `platform_module_state` باستخدام مدرسة ومستخدم فعليين موجودين، ثم تم التراجع عن السجل التجريبي بالكامل.

النتيجة:
- `manager_folder_saved = true`
- `agent_folder_saved = true`
- `manager_folder_name_saved = true`
- `agent_folder_name_saved = true`

وهذا تحقق من أن بيانات المجلد نفسها (`folderId` و`folderName`) قابلة للحفظ والاسترجاع في طبقة الحالة السحابية دون ترك أي بيانات اختبار.
