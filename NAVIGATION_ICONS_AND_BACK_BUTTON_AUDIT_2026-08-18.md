# تدقيق أيقونات الرئيسية والخروج وزر العودة — 2026-08-18

## القاعدة المعتمدة
- تظهر أيقونتا **الرئيسية** و**الخروج** فقط في الصفحة الرئيسية لكل قسم.
- لا تظهر الأيقونتان في الصفحات الداخلية أو صفحات السجلات والنماذج.
- كل صفحة داخلية يجب أن تحتوي زر **رجوع/عودة**؛ وإذا لم يوجد زر مناسب يضيف النظام زر رجوع موحدًا.
- زر الرجوع يستخدم سجل التصفح داخل نفس المنصة أولًا، ثم يرجع إلى الصفحة الرئيسية للدور عند فتح الصفحة مباشرة.

## التنفيذ
1. تعديل `unified_workspace.js` لمنع إنشاء `uwHomeHeader` و`uwExitHeader` خارج الصفحات الجذرية.
2. إضافة `platform-page-navigation.js` لجميع صفحات HTML للتعامل مع الأزرار الثابتة والديناميكية.
3. استخدام `MutationObserver` لمنع إعادة ظهور الرئيسية/الخروج إذا أنشأها سكربت متأخر بعد تحميل الصفحة.
4. الحفاظ على أزرار العودة الموجودة أصلًا وعدم إنشاء زر مكرر.

## نتائج التدقيق
- صفحات HTML المفحوصة: **88**.
- الصفحات الرئيسية/الجذرية: **11**.
- الصفحات الداخلية: **77**.
- الصفحات التي تحمل حارس التنقل: **88/88**.
- ملفات JavaScript الخارجية المفحوصة: **55** — أخطاء Syntax: **0**.
- سكربتات JavaScript المضمنة المفحوصة: **315** — أخطاء Syntax: **0**.
- عقد التنقل الموحد: **PASS**.

## ملاحظة
لم يتم تغيير منطق Supabase أو مركز التكليفات أو الجاهزية أو مصادر البيانات. التغيير خاص بتنظيم شريط التنقل وأزرار العودة فقط.
