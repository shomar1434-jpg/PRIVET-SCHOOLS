# تصحيح موضع زر المراسلات والعودة للقسم

- تم إلغاء المدخل العائم السفلي للمراسلات.
- أصبح `internal-messaging.js` يبحث عن زر «إعدادات مخصصة» في واجهة القسم ويضيف زر «✉️ المراسلات الداخلية» بعده مباشرة مع نفس تنسيق الزر.
- بوابة الموظف الإداري لا تحتوي أصلًا على زر «إعدادات مخصصة» في النسخة الحالية؛ لذلك يظهر زر المراسلات في شريط الأدوات العلوي الخاص بها.
- عداد الرسائل غير المقروءة أصبح مثبتًا على زر المراسلات نفسه.
- فتح مركز المراسلات يمرر `return_to` إلى الصفحة الرئيسية للدور، لا إلى الصفحة الفرعية الحالية.
- زر «العودة للقسم» داخل `internal_messages.html` يقبل فقط صفحات الأقسام الرئيسية المعتمدة، ثم يستخدم دور الجلسة كمسار احتياطي.
- تم التحقق من سلامة JavaScript في `internal-messaging.js` وكتلة JavaScript الداخلية في `internal_messages.html`.
