# تصحيح قسم الموظف الإداري في واجهة مدير النظام

- تم فصل وضع مدير النظام عن وضع الموظف الإداري ووضع المسؤول المباشر.
- بطاقة قسم الموظف الإداري في واجهة مدير النظام تفتح `mode=system_admin`.
- تعرض الواجهة خمسة أقسام داخلية دائمًا: خطة الأداء، التنفيذ، الأعمال المفوضة، التقييم، التحسين.
- لا يتطلب مدير النظام تسجيل دخول كموظف إداري ولا يتم تحويله إلى صفحة دخول الموظف.
- تمرر الصفحات الداخلية `systemAdmin=1` وتحافظ على سياق مدير النظام.
- يوجد زر مستقل لإدارة الموظفين الإداريين عند اختيار مدرسة.
- تم إضافة كسر كاش لـ `index-app.js`.
