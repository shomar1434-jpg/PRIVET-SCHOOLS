# الاعتماد النهائي لنظام المراسلات الداخلية

## الحالة
تم إنهاء مراحل تطوير نظام المراسلات الداخلية المطلوبة على النسخة الحالية، مع مراجعة التكامل وعدم كسر صفحات الأقسام.

## الوظائف المنفذة
- صندوق وارد، مرسلة، مثبتة، وأرشيف.
- البحث والفلاتر حسب النوع والأولوية والحالة.
- الرسائل الفردية والمتعددة بين منسوبي المدرسة.
- إرسال المدير/الوكيل حسب الفئة الوظيفية، وإرسال مدير المدرسة إلى جميع المنسوبين.
- أنواع الرسائل: رسالة داخلية، إشعار، طلب إجراء، تعميم رسمي.
- تأكيد الاطلاع ونسب القراءة والتأكيد لكل مستلم.
- أولوية الرسالة وموعد نهائي اختياري.
- حالات طلب الإجراء: قبول، قيد التنفيذ، تم التنفيذ، رفض.
- الرد، الرد للجميع، إعادة التوجيه، وسلسلة المحادثة.
- تثبيت الرسالة، الأرشفة، وإعادتها للوارد، وتعليمها كغير مقروءة.
- مرفقات من الجهاز عبر محرك الملفات السحابي.
- اختيار مرفقات موجودة مسبقًا من مكتبة القسم دون إعادة رفعها.
- منح مستلم الرسالة صلاحية فتح المرفق المرتبط بالرسالة فقط، حتى لو كان الملف الأصلي خاصًا بصاحبه.
- ربط الرسالة بصفحة/سجل/تقرير/تكليف داخل المنصة وإظهار رابط العنصر.
- زر «إرسال هذه الصفحة» داخل لوحة المراسلات المصغرة في الصفحات الداخلية.
- زر «مراسلة المكلف» داخل مركز التكليفات.
- تحويل الرسالة إلى تكليف فعلي عبر `platform-tasks` للمدير/الوكيل.
- عداد رسائل غير مقروءة في أيقونة المراسلات.
- مسار رجوع يحفظ القسم الذي فتح منه المستخدم مركز المراسلات.

## الصلاحيات والعزل
- كل عمليات Edge Function تتحقق من `platform_sessions`.
- جميع عمليات البيانات مقيدة بـ `school_id`.
- المستخدم العادي يراسل أفراد المدرسة فقط، بحد أقصى 20 مستلمًا في الرسالة.
- المدير/الوكيل يملكان الإرسال حسب الفئة الوظيفية والتعاميم الرسمية.
- الإرسال إلى جميع المنسوبين محصور بمدير المدرسة.
- تحويل الرسالة إلى تكليف محصور بالمدير/الوكيل.
- مركز المراسلات لا يُحقن في سياق مدير النظام.
- صفحات الدخول والتسجيل والإعداد العامة مستثناة من حقن المراسلات.

## الملفات الرئيسية
- `internal_messages.html`
- `internal_messages_app.js`
- `internal-messaging.js`
- `SUPABASE_INTERNAL_MESSAGING.sql`
- `supabase/functions/platform-messages/index.ts`
- تعديل `supabase/functions/platform-files/index.ts` لدعم فتح مرفقات الرسائل للمستلمين المصرح لهم.
- تعديل `.github/workflows/deploy-supabase-functions.yml` لنشر `platform-messages` تلقائيًا.

## نتائج الاختبار الداخلي
- اختبار واجهة متصفح آلي بمحاكاة خدمات Supabase: ناجح بلا أخطاء Console.
- فتح الرسالة وتسجيل القراءة: ناجح.
- تأكيد الاطلاع: ناجح.
- مرفق من مكتبة القسم: ناجح.
- مرفق من الجهاز: ناجح.
- الإرسال الجماعي حسب الدور: ناجح.
- التثبيت والأرشفة: ناجح.
- تحويل الرسالة إلى تكليف: ناجح.
- فحص JavaScript المركزي بـ Node: ناجح.
- فحص 55 صفحة داخلية و223 كتلة JavaScript داخلية: لا توجد أخطاء تركيبية.
- فحص Workflow YAML: ناجح.
- فحص TypeScript لملفي Edge المعدلين: لا توجد أخطاء تركيبية؛ الرسائل الوحيدة محليًا تخص بيئة Deno و`npm:` غير المتاحة لمترجم Node المحلي، وهي متوقعة في Supabase Edge Functions.

راجع `INTERNAL_MESSAGING_QA.json` للتفاصيل الآلية.

## متطلبات التفعيل السحابي
1. تشغيل `SUPABASE_INTERNAL_MESSAGING.sql` مرة واحدة في SQL Editor على مشروع Supabase.
2. رفع النسخة إلى GitHub؛ الـ Workflow مهيأ لنشر `platform-messages` وكذلك سيعيد نشر `platform-files` عند وجود تعديل عليه.
3. بعد النشر، يُجرى اختبار سحابي بحسابين من المدرسة نفسها وحساب من مدرسة ثانية للتحقق من العزل في البيئة الحقيقية.
