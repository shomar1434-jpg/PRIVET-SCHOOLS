# تصحيح فتح أقسام مدير النظام

- التعديل محصور في `index-app.js` فقط.
- أزيل اعتراض مسار أقسام الأدوار الذي كان يحول مدير النظام إلى `private-system-admin-section-preview.html`.
- أصبحت بطاقات: المدير، الوكيل، المعلم، الموجه الصحي، معلمة رياض الأطفال، الموجه الطلابي، رائد النشاط، الموظف الإداري تفتح ملفات الأقسام التشغيلية الأصلية مباشرة.
- عند فتحها من مدير النظام يمرر `mode=system_admin&systemAdmin=1` ويحفظ `smart_school_tab_role_v1=system_admin` حتى لا يتحول مدير النظام إلى هوية مستخدم من الدور.
- لم يتم تغيير صفحات الأقسام نفسها، ولم يتم تغيير `private-school-runtime.js` أو إصلاح «إعدادات مخصصة» المنفذ في المرحلة السابقة.
- فحص JavaScript: `node --check index-app.js` ناجح.
