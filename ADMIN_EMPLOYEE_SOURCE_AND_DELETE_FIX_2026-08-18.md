# تصحيح مصدر الموظفين الإداريين والحذف — 2026-08-18

## السبب الجذري
- رابط الموظف الإداري كان يفتح نموذج الخطة مباشرة، ولا ينشئ حسابًا/عضوية سحابية للموظف.
- بوابة المدير/الوكيل كانت تبني قائمة الموظفين من خطط `localStorage` بدل عضويات المدرسة.
- مصدر معلومات المدرسة كان يسمح باعتبار المستخدم إداريًا من المسمى النصي أو وجود خطة محلية.
- محدد الموظف في بطاقة التقويم كان يلتقطه الربط العام ويضيف مستخدمي المدرسة كافة.

## القاعدة الجديدة
الموظف الإداري هو فقط مستخدم يحمل عضوية صريحة بالدور `administrative_employee` أو `admin_employee` ناتجة من رابط التسجيل الإداري الخاص.

## التنفيذ
- رابط التسجيل الإداري أصبح يفتح `register.html` بدور إداري مقفل ومربوط بالمدرسة وبمرسل الرابط (manager/agent).
- الدور الإداري أُزيل من رابط التسجيل العام.
- التسجيل الإداري ينشئ/يحدّث عضوية `school_members` بالدور الإداري ويحتفظ بجهة الإشراف.
- بوابة وإدارة الموظفين الإداريين تقرآن الحسابات السحابية الصريحة فقط، لا جميع المستخدمين.
- بطاقة التقويم تقرأ نفس القائمة الصريحة، وأُلغي الإنشاء اليدوي للموظفين.
- أضيف حذف الموظف الإداري من بوابة الإدارة والبوابة الإشرافية مع حذف عضويته الإدارية وعدم تحويل بقية المستخدمين إلى إداريين.
- مزامنة حالة التفعيل/التعطيل تمتد لعضوية المدرسة الحالية.

## الملفات المعدلة
1. `supabase-bridge.js`
2. `register.html`
3. `admin_employee_management.html`
4. `administrative_employee_portal.html`
5. `administrative_employee_evaluation.html`
6. `school-information-source.js`

## الفحص
- 55 ملف JavaScript خارجي: صفر أخطاء Syntax.
- 88 صفحة HTML / 315 سكربت JavaScript فعلي: صفر أخطاء Syntax.
- تم استبعاد `<script type="text/plain">` من فحص JavaScript لأنه قالب نصي مشفر وليس كودًا تنفيذيًا.
