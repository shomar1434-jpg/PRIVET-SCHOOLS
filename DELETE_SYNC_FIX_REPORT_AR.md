# تصحيح مزامنة حذف المدرسة

تم نقل الحذف النهائي إلى Edge Function `system-admin` المحمية.

- يتطلب جلسة Supabase Auth لمدير نظام نشط في `system_admins`.
- يحذف كائنات ملفات المدرسة من Storage قبل حذف metadata.
- يحذف صف المدرسة من Supabase؛ الجداول المرتبطة ذات `ON DELETE CASCADE` تنظف تلقائيًا.
- ينظف حسابات Supabase Auth المرتبطة بالمدرسة بعد حذف المدرسة.
- يسجل العملية في `system_admin_audit_log`.
- الواجهة تعرض حالة "جاري الحذف" ثم تعيد جلب قائمة المدارس من Supabase فور نجاح العملية.
- عند فشل الحذف تعيد الواجهة تحميل القائمة الحقيقية من Supabase ولا تخفي المدرسة بشكل زائف.

لا يوجد SQL جديد لهذه الجولة. يلزم إعادة نشر `system-admin` بعد رفع الملف المعدل.
