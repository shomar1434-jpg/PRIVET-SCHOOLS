# تصحيح تكرار بطاقات الأدوات والخدمات

تم تنفيذ تصحيح جذري لآلية تحويل أدوات الشريط القديم إلى بطاقات داخل مساحة العمل.

## سبب المشكلة
النسخة السابقة اعتمدت على تحويل كل عنصر على حدة مع مراقبة DOM. بعض الأقسام تعيد بناء عناصر شريطها أثناء التهيئة، ما يسمح بإعادة إضافة بطاقات الأدوات في دورات متتالية ويظهر صفوفًا مكررة من السحابة والتنبيهات ومحفظة الاجتماعات.

## التصحيح
- إلغاء منطق الإضافة التراكمية للبطاقات.
- جمع الأدوات حسب `id` في قاموس فريد قبل العرض.
- تنظيف أي نسخ مكررة من قسم `uwOperationsSection`.
- إعادة بناء شبكة الأدوات بشكل حتمي باستخدام `replaceChildren()` بحيث توجد بطاقة واحدة فقط لكل أداة.
- منع رابط التسجيل والرئيسية والخروج والإعدادات والمراسلات من الدخول في شبكة الأدوات العامة.
- إزالة `MutationObserver` العام الذي كان يتابع كامل DOM.
- استبداله بمحاولات تهيئة محدودة أثناء بداية التحميل، ثم مزامنة واحدة عند `load`.
- جعل ضغط بطاقات الأدوات الجديدة مستقلاً عن شبكات البطاقات الأصلية للأقسام.

## النتيجة المتوقعة
- قسم أدوات وخدمات واحد فقط داخل كل واجهة رئيسية.
- بطاقة واحدة فقط للسحابة.
- بطاقة واحدة فقط للتنبيهات/إرسال التنبيهات حسب الدور.
- بطاقة واحدة فقط لمحفظة الاجتماعات.
- بطاقة واحدة فقط لقاعة الاجتماعات عندما تكون متاحة للدور.
- عدم المساس بأبعاد بطاقات الأقسام الرئيسية الأصلية.
