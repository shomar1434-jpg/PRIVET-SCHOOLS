# تقرير تصحيح الاستبانات السحابية في نموذج قياس الأثر

## ما تم تصحيحه
- إضافة مكتبة Supabase و`supabase-bridge.js` إلى `impact_assessment.html`.
- إلغاء الاعتماد الأساسي على `localStorage` في إنشاء الاستبانات وقراءة الردود.
- حفظ تعريف الاستبانة في جدول `impact_surveys`.
- حفظ ردود المستفيدين في جدول `impact_survey_responses` عبر دالة RPC عامة آمنة بالرمز.
- حفظ نموذج قياس الأثر نفسه في جدول `impact_assessments`، مع إبقاء نسخة احتياطية للتوافق مع بنك قياس الأثر الحالي.
- توليد رابط عام من نطاق HTTPS الحالي فقط، ومنع إنشاء رابط `file:///`.
- استخدام رمز عام عشوائي `public_token` بدلاً من كشف معرّف داخلي فقط.
- تحميل الاستبانة من Supabase عند فتح الرابط من أي جهاز.
- تحديث التحليلات وعدد الردود من Supabase وربطها بالمؤشر.

## الإجراء المطلوب قبل الاختبار
نفّذ الملف التالي كاملاً مرة واحدة من Supabase SQL Editor:

`SUPABASE_IMPACT_SURVEYS_SETUP.sql`

بعد نجاح التنفيذ، ارفع جميع ملفات النسخة إلى الاستضافة السحابية ثم:
1. ادخل إلى المنصة من رابط المدرسة.
2. افتح نموذج قياس الأثر.
3. أدخل اسم البرنامج وأنشئ الاستبانة.
4. افتح الرابط في نافذة خاصة أو جهاز آخر.
5. أرسل رداً.
6. عد إلى النموذج واضغط تحديث/أعد فتح الصفحة لرؤية الرد والتحليل.

## ملاحظة أمنية
المنصة الحالية تستخدم تسجيل دخول داخلياً ولا تستخدم جلسات Supabase Auth، ولذلك تسمح سياسات جداول الإدارة للواجهة الحالية بالوصول عبر مفتاح anon مع تطبيق عزل المدرسة داخل التطبيق. الربط الوظيفي يعمل، لكن العزل الأمني الصارم على مستوى قاعدة البيانات يتطلب مستقبلاً نقل تسجيل الدخول إلى Supabase Auth ثم ربط سياسات RLS بـ`auth.uid()`.
