# CENTRAL TASK T4 — Analytics Integrity Test

## نطاق الفحص
تم تنفيذ الاختبارين المطلوبين داخل `central_task_center.html`:

1. **Dynamic Card Titles Test**  
   التأكد أن عناوين بطاقات لوحة القيادة لا تبقى ثابتة عند تغيير الفلاتر، بل تتغير وفق:
   - الفترة
   - نطاق المكلفين
   - نوع التكليف
   - حالة القراءة

2. **Shared Query Dataset Test**  
   التأكد أن البطاقات والرسوم والرؤى الذكية تُبنى من نفس مجموعة البيانات المفلترة `analyticsFilteredTasks()`.

## نتيجة الاختبار الأول — عناوين البطاقات
تمت إضافة:

- `analyticsContext()`
- `dynamicCardTitle(base)`
- `dynamicCardSub(base)`

أصبحت عناوين البطاقات تتغير سياقيًا، مثل:

- `التكليفات النشطة — المعلمين / السجلات الكاملة / بانتظار الاعتماد`
- `التكليفات المتأخرة — الإداريين / المهام الجزئية`
- `الإنجاز الموزون — الأدوار الإضافية / المتعثرة`

وبذلك لم تعد العناوين ثابتة أو مضللة عند تغيير الفلاتر.

## نتيجة الاختبار الثاني — Shared Query
تم تثبيت مسار موحد:

```js
const arr = analyticsFilteredTasks();
const m = analyticsMetrics(arr);
```

ثم تستخدم جميع مكونات اللوحة نفس `arr`:

- KPI Cards
- Workload Chart
- Status Donut
- Top Performers
- Load Index
- Bottlenecks
- Weekday Heatmap
- Smart Insights

تمت إضافة وسم مراجعة داخل الدالة:

```js
/* SHARED_QUERY_DATASET */
```

## فحص عدم وجود بيانات افتراضية
عند عدم وجود بيانات مطابقة للفلاتر، تظهر رسالة عدم وجود بيانات بدل أي أرقام ثابتة أو تقديرية.

## فحص سلامة JavaScript
تم استخراج سكربت الصفحة وفحصه بـ:

```bash
node --check tmp_check.js
```

النتيجة: لا توجد أخطاء تركيبية.

## الحكم النهائي
اجتاز مركز التكليفات الاختبارين المطلوبين بعد التصحيح:

- عناوين البطاقات أصبحت ديناميكية.
- البطاقات والرسوم والرؤى أصبحت تعتمد على نفس الاستعلام المفلتر.
