# دليل النشر النهائي

1. ارفع هذه النسخة النهائية إلى GitHub بدل رفع المرحلتين السابقتين.
2. افتح Supabase SQL Editor ونفّذ ملفًا واحدًا فقط:
   `SUPABASE_PLATFORM_FILES_FINAL_SETUP.sql`
3. انشر الوظيفتين:
   - `platform-session`
   - `platform-files`
4. تأكد من وجود أسرار Edge Functions:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
5. سجّل الخروج من المنصة ثم ادخل مجددًا لتوليد جلسة ملفات جديدة.
6. نفّذ اختبار القبول التالي:
   - مدير مدرسة يرفع ملفًا مشتركًا ويعاينه.
   - معلم من المدرسة نفسها يعاين الملف المشترك دون القدرة على تعديله.
   - معلم يرفع ملفًا شخصيًا ولا يظهر لمعلم آخر.
   - مدرسة أخرى لا ترى أي ملف أو مجلد للمدرسة الأولى.
   - إنشاء مجلد ونقل ملف وإعادة تسميته ثم حذفه واستعادته.
   - ربط ملف واحد بسجلين والتحقق من عدم تكرار كائن Storage.
   - استبدال ملف والتحقق من أرشفة الإصدار السابق.
