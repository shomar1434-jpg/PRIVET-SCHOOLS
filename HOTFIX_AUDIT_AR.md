# التصحيح المجمع: تسجيل الوكيل + منطق الرئيسية والخروج

## الملفات التي تُستبدل في نسخة المنصة
1. `private-school-user-register.html`
   - إزالة حقل «تصنيف الوكيل/الوكيلة» من واجهة التسجيل.
   - المستخدم يختار «وكيل/وكيلة» فقط.
   - لا يرسل التسجيل أي `roleVariant`.

2. `manager.html`
   - مدير المدرسة هو المسؤول عن تحديد نوع الوكالة.
   - لا يمكن تفعيل الوكيل قبل تحديد التصنيف من «تعديل».
   - بعد تحديد التصنيف يُحفظ في عضوية المدرسة ويرتبط به منطق سجلات الوكيل.

3. `school_navigation_guard.js`
   - مدير النظام:
     - الرئيسية → `index.html?systemAdminReturn=1&edition=private`
     - الخروج → إنهاء سياق مدير النظام ثم `index.html?signedOut=1&fresh=1`
   - مستخدم المدرسة الخاصة:
     - الرئيسية → الصفحة الجذرية لدوره داخل نفس `schoolId` دون إنهاء الجلسة.
     - الخروج → إنهاء جلسة المستخدم ثم العودة إلى بوابة دخول نفس المدرسة مع `schoolId`.
   - التعرف على الأيقونات يشمل النص/العنوان/aria-label ودوال `goHome/openExitModal`.

## Supabase
تم نشر التعديل الخلفي بالفعل:
- `private-school-registration` → version 2
  - يسمح للوكيل بالتسجيل بدون تصنيف، ويُنشئ العضوية `pending` و`role_variant = null`.
- `private-school-manager` → version 4
  - يضيف `set_member_role_variant`.
  - يمنع تفعيل وكيل لا يحمل تصنيف وكالة صحيحًا.

## اختبارات داخلية
- فحص JavaScript لـ `school_navigation_guard.js`: ناجح.
- فحص JavaScript المضمن في صفحة التسجيل: ناجح.
- تحقق من إزالة `variantField` و`roleVariant` من صفحة التسجيل: ناجح.
- تحقق من وجود الحاجز قبل تفعيل الوكيل في `manager.html`: ناجح.
