# ARCHIVE VIEW CANVAS FIX REPORT

## المشكلة
عند فتح «أرشيف السجلات» و«أرشيف التقويم الذاتي» كانت الواجهة لا تغطي كامل مساحة الشاشة، مما يسمح بظهور خلفية قسم المدير خلف محتوى الأرشيف.

## السبب
واجهتا الأرشيف الداخليتان لم تكونا ضمن قائمة الصفحات التي تُعرض كـ fixed full-screen view، لذلك كانتا تظهران ككتل عادية داخل الصفحة بدل أن تغطيا الشاشة بالكامل.

## التصحيح
- إضافة `#manager-records-archive-view` و `#self-evaluation-archive-view` إلى قواعد العرض الكاملة للشاشة.
- تثبيت الواجهات بـ `position: fixed; inset: 0`.
- إزالة الخلفية الشفافة/الصورة من واجهات الأرشيف الداخلي.
- جعل `content-overlay` بخلفية ثابتة غير شفافة.
- ضبط محتوى الأرشيف بحد أدنى لطول الشاشة حتى لا تظهر فراغات شفافة.

## النطاق
تم تعديل `manager.html` فقط.

## النتيجة المتوقعة
عند فتح أرشيف السجلات أو أرشيف التقويم الذاتي تظهر صفحة كاملة بخلفية ثابتة، ولا يظهر أي جزء من الشاشة الخلفية.
