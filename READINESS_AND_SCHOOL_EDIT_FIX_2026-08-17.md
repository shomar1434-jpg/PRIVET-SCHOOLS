# إصلاح فصل المستخدم عن الفئة الوظيفية وتعديل بيانات المدارس

- حقول الجاهزية `اسم العضو` و`الفئة الوظيفية` مستثناة صراحة من الإثراء التلقائي في `school-information-source.js`.
- اسم العضو يعرض اسم مستخدم المنصة فقط دون إلحاق الدور.
- قائمة الفئة الوظيفية ثابتة وتنتهي عند «أخرى» ولا تقبل إضافة أسماء المستخدمين أو أدوارهم.
- اختيار المستخدم لا يغيّر الفئة الوظيفية تلقائياً.
- قائمة «اختر المسؤول» في المهام غير المسندة تُبنى من مستخدمي المدرسة السحابيين، ويمكن إسناد مهمة إضافية لمستخدم حتى إن لم يكن عضواً مصنفاً في فريق الاستعداد.
- الإسناد اليدوي الإضافي يبقى محفوظاً ولا تمحوه إعادة تصنيف مهام الفئات.
- أضيفت واجهة «تعديل بيانات المدرسة» في إدارة المدارس المستقلة لتعديل اسم المدرسة واسم المدير/ة والبريد الظاهر فقط.
- معرف المدرسة ورمزها ورمز التسجيل وروابط الدخول والتسجيل وحسابات Auth و`users` و`school_members` لا تتغير عند تعديل التسميات.
- البريد الفعلي لهوية تسجيل الدخول يبقى في `manager_email`، بينما البريد المصحح الظاهر في الواجهات يُحفظ في `school_email` حتى لا يؤدي تصحيح التسمية إلى كسر هوية الدخول.
- روابط التسجيل الجديدة تُبنى من `schoolId`/`schoolCode`/`registrationCode` فقط دون تضمين اسم المدرسة، ولذلك لا تتغير عند تصحيح الاسم.

## الفحص
- جميع ملفات JavaScript الخارجية اجتازت `node -c`.
- جميع سكربتات HTML التنفيذية اجتازت فحص Syntax.
- `system-admin/index.ts` لا يحتوي أخطاء تركيبية من فئة TS1xxx.
