# تصحيح مصدر المعلمين — متابعة أعمال المعلمين الأسبوعية

## السبب الجذري
كانت صفحة `deputy_weekly_teacher_followup.html` تعتمد على مفتاح محلي `deputyWeeklyFollowupTeachers:<school_id>` لا يتم إنشاؤه إلا بعد الضغط يدويًا على «تصدير للوكيل» من مركز المعلومات. هذا يفشل عبر الأجهزة والجلسات في المدارس المستقلة، ويجعل توليد الأسابيع يتم بلا قائمة معلمين.

## التصحيح
- المصدر الأساسي أصبح `SchoolInformationSource.getTeachers()` للمدرسة النشطة.
- دعم `school_members` كمصدر عضوية في المدارس المستقلة ومتعددة المدارس، وليس `users.school_id` وحده.
- دمج ملفات `school_teacher_profiles` و`deputy_weekly_teacher_source` عند توفرها لإحضار المادة والحصص والتكليفات التدريسية والإضافية.
- الاحتفاظ بالتصدير المحلي القديم كـ fallback توافق فقط، وليس شرطًا لعمل القسم.
- بعد جلب المعلمين تُنشأ حالات المتابعة تلقائيًا داخل الأسابيع الموجودة مسبقًا، دون الحاجة لإعادة توليد الأسابيع.
- تحديث تلقائي عند أحداث `school-information-ready` و`school-information-updated`.

## نطاق الانعكاس
القائمة الموحدة تغذي: المعلمون، المتابعة اليومية، تكليفات الأسبوع، اختبار الفترة، المهام الإضافية، لوحة التحليل، لوحة الأداء الذكية، التنبيهات، الأرشفة والمقارنة.

## فحص السلامة
- ملفات JavaScript الخارجية: 50 — أخطاء Syntax: 0
- صفحات HTML: 88
- السكربتات المضمنة: 313 — أخطاء Syntax: 0
- الملفات الوظيفية المعدلة فقط: `school-information-source.js`, `deputy_weekly_teacher_followup.html`
