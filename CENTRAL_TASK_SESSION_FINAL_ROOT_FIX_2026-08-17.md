# الإصلاح الجذري النهائي لجلسة مركز التكليفات — 2026-08-17

## السبب المثبت من السجلات الحية
- إنشاء التكليف ينجح (`platform-tasks?action=create` يعيد 201).
- بعد ذلك قد تستخدم طبقة أخرى رمز `platform-session` قديمًا ألغته آلية الجلسات السابقة، فتعود `platform-session` بـ401.
- مسار التوافق القديم كان يحاول إعادة تسجيل الدخول بكلمة مرور محفوظة محليًا وينتج `Invalid login credentials` ورسالة حمراء مضللة.

## التصحيح
1. حذف fallback كلمة المرور نهائيًا من `platform-cloud-session.js`.
2. `renew` يبحث عن الرمز حتى لو كان `revoked` بدل حصر البحث في `status=active`.
3. الرمز الملغي لا يُستعاد إلا إذا:
   - لم تنتهِ صلاحيته الأصلية؛
   - توجد جلسة شقيقة نشطة وغير منتهية لنفس المستخدم والمدرسة والدور؛
   - عضوية المستخدم في المدرسة لا تزال فعالة.
4. عند تحقق الشروط يصدر الخادم رمزًا جديدًا ولا يعيد استخدام الرمز القديم.
5. Workflow يحتوي نشر `platform-session`.

## التحقق
- 54 ملف JavaScript خارجي: بدون أخطاء Syntax.
- 316 سكربتًا مضمنًا داخل HTML: بدون أخطاء Syntax.
- TypeScript: لا توجد أخطاء Parse من فئة TS1xxx؛ تحذيرات Deno/import المحلية متوقعة خارج Supabase.
- تحقق حي: توجد جلسات نشطة للمستخدم/المدرسة/الدور نفسه مع رموز قديمة revoked، وهي الحالة المستهدفة بالإصلاح.
