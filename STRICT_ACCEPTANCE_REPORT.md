# تقرير القبول الصارم — المراسلات والأرشيفات وقرارات التكليف

التاريخ: 2026-08-26
النطاق: نسخة المدارس المستقلة المرفوعة باسم `تجارب.zip`.

## 1. نظام المراسلات والمرفقات
تم تغيير دورة الإرسال بحيث لا تعتبر الرسالة ناجحة إلا بعد تحقق الخادم من الآتي بالترتيب:
1. صلاحية المستلم داخل المدرسة.
2. صلاحية كل ملف مرفق ووجوده في `platform_files` قبل إنشاء الرسالة.
3. إنشاء الرسالة.
4. إنشاء جميع المستلمين.
5. إنشاء روابط `platform_file_links` للمرفقات.
6. إنشاء صفوف `internal_message_attachments`.
7. إعادة قراءة المرفقات ومطابقة معرفاتها واحدًا بواحد مع المرفقات المطلوبة.
8. إعادة التحقق من وجود الرسالة وعدد المستلمين.

في حالة فشل أي خطوة بعد إنشاء الرسالة، تنفذ الوظيفة تنظيفًا تعويضيًا يحذف صفوف المرفقات والروابط والمستلمين والرسالة، ولا تعيد `ok:true`.
الواجهة لا تغلق نافذة الإنشاء ولا تعرض نجاح الإرسال إذا لم يرجع الخادم `attachmentsVerified:true` مع العدد والمعرفات المطابقة.

تم نشر `private-school-messages` على Supabase كالإصدار **7** وحالته **ACTIVE** مع `verify_jwt=true`.

## 2. تقارير الأداء الوظيفي
أضيفت طبقة تحقق نهائية بعد جميع أغلفة الحفظ القديمة، وتقوم قبل الحفظ بـ:
- تثبيت القيم الحالية للقوائم المنسدلة في HTML المؤرشف.
- حفظ حالة الحقول.
- حماية الأهداف التفصيلية المكتوبة/المختارة من المستخدم من الاستبدال الآلي.
- حفظ `formState`, `savedDropdowns`, `manualDetailedGoals` مع التقرير.
- التحقق من وجود التقرير في الأرشيف المحلي ثم التحقق من المزامنة السحابية.

تم تطبيقها على: المدير، الوكيل، المعلم، الموجه الطلابي، رائد النشاط، الموجه الصحي، ومعلمة رياض الأطفال.

## 3. أرشيف سجلات المدير
تم تعديل `manager_records.html` و`manager_library_records.html` بحيث:
- ينشأ لكل نسخة معرف سجل ثابت وفريد.
- لا تظهر رسالة نجاح سحابية قبل التحقق من وجود السجل في `school_manager_records_archive_v1` ومن المزامنة.
- عند فشل التحقق يبقى الحفظ المحلي ولا تضيع البيانات، لكن تظهر رسالة تحذير صريحة بدل نجاح وهمي.

كما أضيف التحقق إلى محرك الأرشيف الموجود في `manager.html` دون حذف أي مفاتيح أو سجلات قديمة.

## 4. الخطة التشغيلية واللجان والفرق وأرشيف التقويم الذاتي
كان الحفظ السابق ينشئ معرفًا جديدًا زمنيًا عند كل ضغط حفظ، ولذلك كان التعديل يتحول إلى نسخة مكررة. تم تحويله إلى Upsert بمعرف ثابت حسب نوع السجل والسياق.
- الخطة التشغيلية: تحفظ/تحدث نفس سجل العام.
- قرار اللجنة/الفريق: يحفظ/يحدث حسب معرف اللجنة.
- اجتماعات اللجنة/الفريق: يحفظ/يحدث حسب معرف اللجنة، ولا تستبدل لجنة اجتماعات لجنة أخرى.
- يتم إرسال الحالة إلى النافذة الأم ثم `PlatformPersistenceGuard.flush()`.
- لا تظهر رسالة نجاح نهائية في واجهة التقويم الذاتي إلا بعد ACK من الأرشيف الأم والتحقق منه.

## 5. إرسال قرار التكليف للمكلف
بعد حفظ قرار التكليف:
- يتم حفظ `assigneeUserId`, الدور والبريد ضمن metadata للقرار إن أمكن مطابقة الاسم بحساب المدرسة.
- ظهر زر `إرسال للمكلف` في سجل القرار وكذلك داخل مجلد القرارات والتكليفات عند المدير.
- عند الضغط، يتم فتح نظام المراسلات الأصلي نفسه بوضع إنشاء رسالة، مع تحديد حساب المكلف والعنوان والنص وربط قرار التكليف وإرفاق ملف القرار المحفوظ نفسه.
- يمر الإرسال بعد ذلك بدورة التحقق الصارمة نفسها للمرفقات.

## الاختبارات المنفذة
### فحص الشيفرة
- `node --check`: نجح لـ `internal_messages_app.js`, `archive-save-integrity.js`, `performance-report-save-integrity.js`.
- TypeScript parser: نجح لـ `private-school-messages/index.ts`.
- فحص inline JavaScript: نجح لنموذج القرارات، محرك أرشيف المدير، سجلات المدير، والجسر الخارجي للتقويم الذاتي.
- تم فك `srcdoc` الخاص بتطبيق Vue في التقويم الذاتي وفحص سكربتاته داخليًا ونجح الفحص.

### اختبار الواجهة المنطقي للمراسلات
تم تشغيل محاكاة فعلية لدالة `send()`:
- رد خادم `ok:true` لكن `attachmentsVerified:false` => رُفض النجاح وبقيت نافذة الإرسال مفتوحة.
- رد خادم موثق مع معرف المرفق المطابق => تم قبول النجاح وإغلاق نافذة الإرسال.
النتيجة: `unverifiedRejected=true`, `composeStayedOpen=true`, `verifiedSuccessClosed=true`.

### اختبار حفظ تقارير الأداء
تم تشغيل محاكاة فعلية لمحرك الحفظ النهائي:
- حفظ التقرير: نجح.
- القيمة المختارة من القائمة المنسدلة `b`: عادت كما هي.
- الهدف التفصيلي اليدوي: بقي كما كتبه المستخدم.
النتيجة: `saved=true`, `dropdown=b`, `manualGoal=true`.

### اختبار محرك الأرشيف
تم تشغيل محاكاة فعلية لـ `ArchiveSaveIntegrity`:
- وجود السجل المحلي: مؤكد.
- المزامنة السحابية المحاكية: مؤكدة.
- طلب سجل غير موجود: مرفوض كما يجب.

### اختبار قاعدة Supabase الفعلية — داخل Transaction ثم ROLLBACK
لم تترك الاختبارات أي بيانات تجريبية.
- مسار المراسلات: `message_saved=true`, `recipient_saved=true`, `attachment_saved=true`, `link_saved=true`, `attachment_matches_file=true`.
- مسار حالة الأرشيف: `saved_and_found=true`, `update_verified=true`, `edit_verified=true`.

### اختبار رابط قرار التكليف بالمراسلات
تم اختبار مولد الرابط:
- حساب المكلف مرر كمستلم: ناجح.
- ملف القرار مرر كمرفق: ناجح.
- فتح وضع إنشاء رسالة: ناجح.

## حماية البيانات القديمة
- لم يتم حذف أي سجل مدرسة قائم.
- لم يتم تغيير أسماء مفاتيح الأرشيفات القديمة.
- لم يتم تنفيذ Migration إتلافي.
- اختبارات قاعدة البيانات نفذت داخل `ROLLBACK`.
- التغييرات تعمل فوق البيانات الحالية بطريقة Upsert/Verify، مع الإبقاء على السجلات السابقة.

## ملاحظة القبول
الاختبارات أعلاه هي اختبارات شيفرة + محاكاة تنفيذية + معاملات حقيقية على قاعدة البيانات + نشر فعلي لوظيفة المراسلات. لم يتم استخدام حساب مستخدم بشري في متصفح لتسجيل الدخول كمرسل ثم كمستلم، لأن جلسة حساب المدرسة الخاصة بالمستخدم غير متاحة للأداة. لذلك لا يتم الادعاء بإجراء اختبار بشري عبر واجهتين، لكن المسار الخادمي والعلاقات وقواعد قبول/رفض النجاح تم اختبارها فعليًا.
