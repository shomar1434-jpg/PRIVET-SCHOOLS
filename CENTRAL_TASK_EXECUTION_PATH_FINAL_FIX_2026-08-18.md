# إصلاح مسار تنفيذ مركز التكليفات — 2026-08-18

## السبب الجذري
كان يوجد أكثر من مدير للجلسة في الصفحة نفسها. `PlatformCloudSession` يدير `platform_file_session_token`، بينما `agent-core-v2.js` كان يستطيع حذف المفتاح نفسه عند 401 ثم محاولة استعادة الجلسة بمسار قديم (`bootstrap_auth` ثم بيانات دخول محلية). أدى ذلك إلى كسر جلسة مركز التكليفات من محرك ثانوي بعد إصلاحها في المحرك الرئيسي.

كما كان `currentSchoolId()` يفضل معرف المدرسة المخزن داخل جلسة قديمة على معرف المدرسة النشطة، وهو غير صحيح مع تعدد المدارس.

## التصحيح
- توحيد إدارة الجلسة تحت `PlatformCloudSession` فقط.
- منع `agent-core-v2` من حذف أو إنشاء رمز جلسة مستقل أو الرجوع لكلمة المرور.
- جعل المدرسة النشطة المصدر الأول للحقيقة عند الاستعادة.
- عدم اعتبار رمز صالح زمنيًا صالحًا وظيفيًا إذا كان تابعًا لمدرسة غير المدرسة النشطة.
- إضافة بوابة تهيئة للجلسة قبل أول `refresh()` في مركز التكليفات.
- دعم `bootstrap_auth` كاسم توافق قديم على الخادم مع توجيهه إلى مسار `auth-recover` الآمن لمنع كسر نسخ JavaScript مخزنة مؤقتًا.
- رفع أرقام cache-busting لملفات الجلسة والتكليفات والمحول وAgent Core في صفحة مركز التكليفات.

## اختبارات داخلية
- محاكاة رمز جلسة لمدرسة A مع مدرسة B نشطة: نجح الاسترداد وأصبح الرمز مرتبطًا بالمدرسة B.
- التحقق أن Agent Core لا يحذف `platform_file_session_token`: ناجح.
- التحقق أن مسار Agent Core لا يحتوي `bootstrap_auth` أو password fallback: ناجح.
- 49 ملف JavaScript خارجي: 0 أخطاء Syntax.
- 312 سكربتًا مضمنًا: 0 أخطاء Syntax.
- TypeScript `platform-session`: 0 أخطاء تركيبية TS1xxx.
- جميع مراجع سكربتات `central_task_center.html` المحلية موجودة.
- Workflow يتضمن نشر `platform-session` ويستجيب لتغييرات مساره.

## ملاحظة النشر
يجب نجاح GitHub Action الخاص بـ Deploy Supabase Edge Functions بعد رفع النسخة لأن التصحيح يتضمن تحديث `platform-session`.
