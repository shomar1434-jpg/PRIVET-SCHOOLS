# إصلاح زر حفظ التكليف السحابي

## السبب الجذري
كان نموذج مركز التكليفات يربط حدث الإرسال بالمعالج المحلي `createTask` قبل تحميل محول التكليفات السحابي. بعد ذلك كان المحول يستبدل `window.createTask`، لكن مستمع النموذج ظل يشير إلى الدالة المحلية القديمة؛ لذلك لم يصل أي طلب إلى `platform-tasks`، ثم كانت المزامنة السحابية تعيد تحميل قائمة فارغة فتبدو العملية وكأن الزر لا يعمل.

## التصحيح
- جعل مستمع النموذج يستدعي `window.createTask` وقت الضغط، وليس نسخة قديمة من الدالة.
- إزالة مستمع الحفظ المحلي وإعادة ربط النموذج صراحة بالمعالج السحابي.
- إضافة منع النقر المكرر وحالة «جارٍ الحفظ سحابيًا».
- إضافة تحقق واضح من المستخدم المكلف.
- إعادة تحميل التكليفات من Supabase بعد نجاح الإنشاء.
- إبقاء رسائل الخطأ الفعلية القادمة من Edge Function ظاهرة للمستخدم.

## النتيجة المتوقعة
عند الضغط على «حفظ التكليف» يصل طلب POST إلى `platform-tasks`، ثم تُنشأ السجلات في `central_tasks` والجداول المرتبطة، ويظهر التكليف للمستخدم المكلف بعد تحديث حسابه.
