# تقرير التحقق الصارم — قسم التقويم الذاتي

تم اكتشاف أن الزر العلوي «حفظ في أرشيف التقويم الذاتي» كان عامًا ومبهمًا، بينما يغيّر وجهته خفيًا بحسب `currentTab`. والأسوأ أن الدالة نفسها `saveToLocalStorage()` كانت تُستدعى أيضًا من عمليات الحفظ التلقائي والاستيراد والتهيئة، ولذلك لم يكن الفصل واضحًا بين «حفظ حالة العمل» و«إيداع سجل رسمي في الأرشيف».

## الوجهات الجديدة
- بيانات المدرسة → `selfEval_06` — بيانات عامة عن المدرسة.
- واقع المدرسة الفعلي → `selfEval_56` — واقع المدرسة الفعلي.
- الخطة التشغيلية → `selfEval_53` — الخطة التشغيلية.
- قرار المجلس/اللجنة/الفريق الحالي → `selfEval_54` — قرارات المجالس واللجان والفرق.
- الاجتماع الحالي فقط → `selfEval_55` — اجتماعات المجالس واللجان والفرق.
- بوابة التميز والمؤشرات → `selfEval_57` — بوابة التميز والمؤشرات.

تم إلغاء وظيفة زر الأرشفة العام، وأصبح لكل قسم زر مستقل واضح.

## دورة الحفظ
زر القسم → حفظ حالة العمل → إرسال `SELF_EVAL_ARCHIVE_REQUEST` → تحقق الوجهة → Upsert في الأرشيف الداخلي → Upsert في أرشيف المدير → إعادة القراءة والتأكد من المجلد → مزامنة الحالة السحابية → مقارنة المحلي بالسحابي → ACK نجاح → عندها فقط تظهر رسالة النجاح.

## عدم التكرار
الخطة التشغيلية وبيانات المدرسة والواقع والمؤشرات تستخدم سجلًا ثابتًا لكل عام. قرار كل مجلس/لجنة/فريق يستخدم معرف الجهة. كل اجتماع يستخدم معرفًا مستقلًا وثابتًا. إعادة الحفظ تعدل السجل نفسه بدل إنشاء نسخة زمنية جديدة.

## الاختبار التنفيذي الداخلي
`{"destinations":6,"operationalPlanUpsert":true,"invalidDestinationRejected":true,"managerRows":6,"cloudVerified":true}`

تم اختبار الوجهات الست كلها، ثم تعديل الخطة وإعادة حفظها والتأكد من بقاء سجل واحد فقط وتحديث بياناته، وتم رفض وجهة غير معروفة.

## سلامة السكربت
نجح فحص JavaScript للمحرك الخارجي وجميع السكربتات التنفيذية داخل `committeesFrame` بعد التعديل.
