# اعتماد المرحلة الأولى — إصدار المدارس الأهلية والخاصة

التاريخ: 2026-08-21
النسخة المرجعية الوحيدة للتطوير: `تجارب (3).zip`
النسخة العامة: استخدمت للقراءة والمقارنة المنطقية فقط، ولم يُنقل أو يُربط منها أي ملف بالنسخة الخاصة.

## نطاق المرحلة الأولى المكتمل
1. تسجيل مدرسة جديدة من مدير النظام في مشروع المدارس الخاصة.
2. إنشاء المالك وربطه بالمدرسة كمالك أساسي وعضو نشط.
3. توليد رابط دخول مالك مرتبط بـ `schoolId`.
4. منع دخول المالك عبر رابط مدرسة لا يملكها.
5. تحميل بوابة المالك من سياق Supabase الفعلي.
6. توليد دعوة تسجيل مدير من بوابة المالك.
7. فحص الدعوة وقبولها دون جلسة Auth مسبقة، مع Token وانتهاء صلاحية.
8. إنشاء المدير بحالة `pending` ومنع جلسة المدرسة قبل التفعيل.
9. ظهور المدير المعلق لدى المالك.
10. تفعيل المدير من المالك مع توثيق `activated_at` و`activated_by`.
11. توليد رابط دخول المدير المرتبط بالمدرسة.
12. السماح للمدير النشط فقط بإنشاء جلسة مدير للمدرسة الصحيحة.
13. منع عبور المالك أو المدير إلى مدرسة أخرى.
14. فصل علامات جلسة مدير النظام عن جلسات مستخدمي المدرسة.
15. منع مسح التخزين المحلي بالكامل عند الخروج.

## إصلاحات جذرية نفذت
- تصحيح واجهة دخول مدير النظام المستقلة بدل إعادة المستخدم إلى `index.html` عند غياب الجلسة.
- تصحيح ترتيب Bootstrap بحيث يتم تحميل `standalone-private-config.js` قبل `private-school-config.js` والجسر.
- تنظيف علامات `system_admin_*` عند الانتقال إلى جلسة مدرسة.
- إلزام دخول المالك بمعرف المدرسة الموجود في رابط الدخول والتحقق من مطابقته للسياق المسترجع.
- الحفاظ على `schoolId` عند خروج المالك وإعادته إلى صفحة الدخول.
- توثيق تفعيل وتعطيل المدير زمنيًا وهوية المنفذ.
- منع إنشاء مدير نشط/معلق مكرر بنفس البريد داخل المدرسة.
- تثبيت `private-school-invite` كوظيفة Token-based لا تتطلب JWT قبل إنشاء الحساب.
- مزامنة مصادر وظائف المرحلة الأولى المحلية مع المنطق المنشور حتى لا يعيد GitHub نشر نسخة خلفية قديمة.
- فصل نشر Edge Functions عن Migrations؛ لا يوجد `supabase db push` تلقائي في Workflow الوظائف.

## اختبارات داخلية ناجحة
- JavaScript syntax للملفات التشغيلية: PASS.
- فحص روابط واعتماديات صفحات المرحلة حتى `manager.html`: PASS، صفر ملفات مفقودة.
- Phase-1 regression contracts: PASS (`PHASE1_REGRESSION_CONTRACTS_OK`).
- عدم وجود `localStorage.clear()` في مسارات المرحلة: PASS.
- اختبار دورة بيانات حقيقية داخل قاعدة Supabase بمعاملة تراجعية: إنشاء مدرسة + مالك + دعوة مدير + مدير pending + تفعيل: PASS.
- اختبار العزل الحقيقي في قاعدة Supabase: المالك لمدرسته فقط، منع cross-school، المدير pending غير نشط: PASS.
- اختبار حقول تدقيق تفعيل/تعطيل المدير: PASS.
- التحقق من عدم بقاء بيانات الاختبار بعد Rollback/cleanup: PASS، صفر سجلات اختبار متبقية.
- نشر `private-school-owner` v5 في مشروع المدارس الخاصة: PASS.
- نقطة اختبار E2E المؤقتة تم إيقافها بعد المراجعة وإعادة نشرها محمية بـ JWT وتعيد 410؛ ليست جزءًا من المنصة.

## حماية النشر
- `.github/workflows/validate.yml` يشغل اختبار المرحلة الأولى تلقائيًا.
- `.github/workflows/deploy-supabase.yml` ينشر الوظائف فقط ولا يعدل Schema.
- `.github/workflows/deploy-supabase-migrations.yml` يدوي ومقصود للمراجعة فقط.
- `PHASE1_CHANGED_FILES.sha256` يحتوي بصمات ملفات التغيير الحرجة.

## ملاحظة اختبار بيئة النشر
بيئة الأدوات الداخلية لا تسمح باتصال Chromium المحلي أو DNS الخارجي المباشر؛ لذلك لم يُسجل اختبار متصفح GitHub Pages كاختبار داخلي ناجح. هذا لا يُخفى في التقرير. بعد رفع هذه الحزمة، اختبار المتصفح المنشور هو اختبار قبول النشر (Deployment/UAT) على نفس الكود الذي اجتاز الاختبارات البرمجية وقاعدة البيانات، وليس مرحلة تطوير إضافية.
