# JS / Routing Root-Cause Hotfix Report

## القرار
تم رفض نسخة S1.3 السابقة والرجوع إلى آخر نسخة مستقرة قبل الترحيل، ثم إصلاح سبب ظهور الأكواد على الواجهة بدل معالجة الأعراض فقط.

## التصحيحات المنفذة
1. `teacher.html`: إزالة سكربت كان مدمجًا داخل قالب ملف الإنجاز المصدر بصيغة HTML؛ كان يحتوي على وسم `</script>` فعلي داخل JavaScript مما تسبب في إغلاق السكربت مبكرًا وظهور الكود أعلى واجهة المعلم.
2. `manager.html`: تغليف كتلة CSS كانت ظاهرة كنص داخل الصفحة داخل وسم `<style>`.
3. `manager.html`: إضافة ربط آمن ومباشر لبطاقتي `أرشيف السجلات` و`أرشيف التقويم الذاتي` دون تغيير منطق السجلات أو الصلاحيات.

## حدود النسخة
هذه نسخة إصلاح جذرية مبنية على آخر Stable، وليست استمرارًا لنسخة S1.3 المرفوضة.

## النتيجة المتوقعة عند الاختبار
- عدم ظهور كود JavaScript أعلى واجهة المعلم.
- فتح أرشيف السجلات وأرشيف التقويم الذاتي في قسم المدير عند الضغط عليهما.
- عدم ظهور CSS كنص داخل واجهة المدير.
