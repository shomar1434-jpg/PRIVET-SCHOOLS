# اعتماد دورة الموظف الإداري المستقلة — 2026-08-18

تم تنفيذ الدورة التالية على آخر نسخة مستقرة للمنصة:

1. التسجيل من رابط الموظف الإداري فقط بدور `administrative_employee` وحالة `pending`.
2. ظهور الموظف للمدير حتى لو كان رابط التسجيل منشأ من الوكيل، مع بقاء علامة جهة الإشراف.
3. التفعيل متاح لمدير المدرسة فقط، ويحدّث `users` و`school_members` إلى `active`.
4. واجهة دخول مستقلة `administrative_employee_login.html` لا تقبل أي دور آخر، وترفض الحساب غير المفعّل.
5. بوابة المدرسة العامة تحوّل الموظف الإداري إلى واجهة الدخول المستقلة بدل فتح قسمه من واجهة بقية المستخدمين.
6. خطة الأداء تُحفظ سحابيًا في `platform-state` ضمن `admin_performance` بنطاق المستخدم.
7. مدير المدرسة يقرأ خطط الموظفين الإداريين على مستوى المدرسة ويعتمد/يعيد الخطة إلى نطاق الموظف نفسه.
8. الخطة المرسلة (`pending`) أو المعتمدة (`approved`) لا يمكن للموظف تعديلها؛ التنفيذ لا يبدأ إلا بعد `approved`.
9. صفحات التنفيذ والتقييم والتحسين تدعم `owner_uid` حتى يراجع المدير بيانات الموظف السحابية نفسها بدل بيانات حساب المدير.
10. الموظف الإداري لا يظهر في قائمة المكلفين بمركز التكليفات إلا إذا كانت عضويته `active`.
11. التكليفات الإضافية تظهر داخل بوابة الموظف الإداري من `platform-tasks` السحابي وتنفذ عبر دورة التكليفات الموحدة.

## ملفات Edge Functions المتغيرة
- `supabase/functions/platform-state/index.ts`
- `supabase/functions/platform-tasks/index.ts`

يجب نجاح نشر الوظيفتين في GitHub Actions.

## اختبارات العقد
تم اجتياز 19 تحققًا وظيفيًا ثابتًا تشمل: التسجيل المعلّق، التفعيل، حارس الدور، حارس الحالة، الجلسة السحابية، فصل واجهة الدخول، تجميع الخطط السحابي، اعتماد المدير الموجه لنطاق الموظف، قفل الخطة قبل/بعد الاعتماد، إدراج الموظف المفعّل فقط في التكليفات، وربط بطاقة التكليفات بمحرك `platform-tasks`.

تم فحص JavaScript المعدل باستخدام `node --check`. فحص TypeScript لم يظهر أخطاء تركيبية TS1xxx؛ التحذيرات المحلية اقتصرت على عدم توفر أنواع Deno و`npm:@supabase` في بيئة الفحص المحلية.
